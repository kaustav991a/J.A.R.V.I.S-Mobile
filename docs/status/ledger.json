{
  "$comment": [
    "THE SINGLE SOURCE OF TRUTH for every status claim in this repo.",
    "",
    "Nothing else may carry one. ROADMAP.md §0b/§0c and docs/completion-tracker.html are",
    "GENERATED from this file by `node scripts/build-status.mjs` — edit here, never there, or the next",
    "generate silently reverts you. RESUME.md is untouched by the generator: it answers a",
    "different question (how something was proved, and what it cost to find out) and is",
    "append-only archaeology.",
    "",
    "status  — proved:   a human has seen it work on the phone",
    "          partial:  works, with a named gap",
    "          untested: code and tests are in, no human has ever exercised it",
    "          broken:   works badly, defect logged in ROADMAP §2",
    "          none:     not built",
    "",
    "blockedBy — what stands between this row and being finished. This is the field that",
    "          keeps the brain dependency visible rather than buried in prose, and it is",
    "          why the generator can say 'the app alone cannot reach 100%' with a number",
    "          instead of an opinion.",
    "          brain:     needs jarvis-brain — a gateway route, a merge, or a deploy",
    "          desk:      needs the desk machine on",
    "          device:    needs the phone in hand, unlocked (adb cannot unlock it)",
    "          app-build: app repo, but needs a native build or an EAS credentials action",
    "          app:       app repo, ships over the air",
    "          none:      finished, nothing owed",
    "",
    "FROZEN 2026-08-29 by Kaustav: NO further work on this app until the DESK is 100%.",
    "Not a blocker that arrived - a scope decision. Everything below stays true and stays",
    "unfinished; nothing here is picked up, and the OTA publish that would put the last two",
    "app commits on the phone waits with it. The desk tracker owns the decision and the",
    "definition: F:/work/JARVIS-Project/JARVIS_TRACKER.md section 0.5."
  ],
  "meta": {
    "repo": "jarvis-mobile",
    "read": "2026-09-01",
    "branch": "feat/mobile-hud",
    "head": "87fd15a",
    "tests": 1251,
    "suites": 88,
    "typecheck": "clean",
    "sources": {
      "status": "this file",
      "archaeology": "RESUME.md",
      "queue": "docs/superpowers/plans/2026-08-24-app-completion.md",
      "device script": "TESTING.md"
    },
    "otaVerified": {
      "on": "2026-09-01",
      "channel": "production",
      "channelId": "01a01a04-f0d6-7718-90dc-dbae6930b0db",
      "runtime": "31c64113d7d13a400eb1c56ef81c4d0d4be3fa17",
      "group": "33ab8d55-2ea9-4af5-b3f3-1437d8f46b3c",
      "note": "Checked against the phone on every publish since 2026-08-24, when a publish went to a runtime no device had. Runtime is byte-identical to the fingerprint the installed APK carries, so a JS-only publish reaches the phone. A LOCAL build still receives nothing unless the fingerprint is baked by hand. Checked again on 2026-08-27, before the publish rather than after: `fingerprint:generate` returned the same 31c64113 the APK carries, and update 01a0422d went out to it. Twice on 2026-08-27, fingerprint checked before each: 01a0422d (the bell and the tab root), 01a04293 and 01a0429e (the chat order, in two halves) and 01a042b3 (the interrupted turn). **2026-09-01: eleven publishes in one day, every one checked against the phone first, and the runtime never moved.** Everything shipped today was JavaScript — the crash record, the anticipation rebuild, the place accuracy, the map panel and its removal, and the two diagnostic levers. The last landed update is `01a05c5a`."
    },
    "hold": "frozen 2026-08-29 until the desk is 100% - see JARVIS_TRACKER.md 0.5"
  },
  "blockers": {
    "brain": {
      "label": "Brain",
      "detail": "jarvis-brain — a gateway route, a merge, or a deploy. **Closed as of 2026-08-26 and deliberately not being touched;** that repo is clean and fully pushed. The long form of every row here, and what each costs when it is opened again, is in `docs/brain-dependencies.md`."
    },
    "desk": {
      "label": "Desk",
      "detail": "Needs the desk machine on. This laptop has no Python — `python` opens the Microsoft Store."
    },
    "device": {
      "label": "Phone",
      "detail": "Needs the phone in hand and unlocked. `adb` can wake the display but cannot unlock it, so the app launches behind the keyguard and never foregrounds — `apps_linked: 0` in that state means nothing."
    },
    "app-build": {
      "label": "App · build",
      "detail": "App repo, but needs a native build or an EAS credentials action rather than an over-the-air publish."
    },
    "app": {
      "label": "App",
      "detail": "App repo, ships over the air. Nothing outside this repo is owed."
    },
    "none": {
      "label": "Done",
      "detail": "Nothing owed."
    }
  },
  "deferred": {
    "brain": {
      "label": "Brain",
      "repo": "jarvis-brain",
      "since": "2026-08-26, and reopened for weekends from 2026-08-31",
      "head": "462fa4c on `feat/cloud-gateway`, which is what Render watches",
      "why": "Closed on this laptop, worked on the home machine. The split is hardware, not preference: there is no Python here, so nothing in `cloud_gateway.py` can be run, imported or tested from this side. The weekend of 2026-08-29 proved the handoff works — items 24 and 25 were both picked up from this page and fixed there.",
      "warnings": [
        "**Read this page as of 2026-08-31, not as of the week before.** Four of its warnings were retired on the weekend of 08-29 and are gone: `fix/durable-state` is merged (`c86d176` is contained in `origin/feat/cloud-gateway`), so a deploy no longer disarms the briefing; the local checkout is no longer 42 commits behind; and **item 24, the invented distances, is fixed** (`efd1af1`). What is left below is genuinely left.",
        "**This laptop still cannot run gateway work at all.** `python` and `python3` resolve to the Microsoft Store stub and `py` does not exist, so `pytest` cannot run and `cloud_gateway.py` cannot be imported. Anything written here would be written blind. **The home machine is the only place any of this is testable**, which is why this page exists in the app repo at all.",
        "**Two fixes are written and not proved on the phone**, and both will look like live app defects until an OTA publish goes out from this side. Item 25 (the transcript that outlives its socket) is live on the gateway and committed here as `6d8be2d`; item 22 (the briefing rotation) is `02604c6` on the gateway. **Do not re-diagnose either from the phone** — they are waiting on a publish, not on a fix.",
        "**The capability-token migration is not finished, it is merely started.** The gateway mints per-door tokens and still accepts the master; the app half shipped as `38a05cb`. It is finished when `/health.app_auth.master_calls` goes quiet after the phone has published and run for a day — and only then can the master be retired. Retiring it before that locks the phone out of its own gateway."
      ],
      "buildableAnyway": [
        "**Queue 11, the notification listener** — the spec and the pure drop-rule reducer, with tests, with no service registered and no permission asked. The gating on 12 is a privacy decision, not a sequencing one: a listener sees OTPs, balances and private messages, and once that is collected badly no later fix un-collects it.",
        "**Queue 20's app half** — derive facts from turns, keep a ledger of what was sent, supersede what changed, send derived facts and never raw rows. `shareFacts` is the proved shape to copy. Consent rule comes first. Only the brain half is blocked: the ask envelope carries **one turn**, so he answers cold even though `chat_turns` holds the history."
      ],
      "owedNext": [
        "**15 — a per-message id.** Checked against `origin/feat/cloud-gateway` on 2026-08-31: there is no `msg_id` / `message_id` anywhere in `cloud_gateway.py`, so delivered and read ticks cannot start on this side at all. The app knows *sent* and nothing else. **Reply-to-a-message wants the same id**, so this unblocks two features rather than one, and inventing a second identity for a message later is the thing to avoid. Highest value of anything left here.",
        "**presence — \"awake but idle\", and the app half cannot start without a field to put it in.** Inferred from socket state today, which cannot tell a phone in a pocket from a phone being read. The phone already has the honest signal and sends none of it: the journal holds `screen_on` / `screen_off` / `unlock` timestamps, so minutes-since-last-interaction is a derived number the app can put on the ask envelope the moment the gateway accepts one. **Derived only, never raw** — a count of minutes, never what was on screen. Name the field and the app half follows in a sitting.",
        "**anticipate-pocket — the remark that finds you.** `anticipate()` decides in code, on the phone, and is measurably incapable of running unattended on this device (`#netAvail=0`, RARE bucket). So the gateway has to be the sender. The cheapest shape that does not duplicate the judgement: the phone posts the remark it would have made along with its own budget marker, and the gateway pushes it when it next has a reason to — rather than the gateway deciding what is worth saying, which is the mistake `speaks-first` already paid for. One remark a day, shared with the nudge budget, or the two channels talk over each other.",
        "**6 — the transcribe prompt overcorrects on plain English.** `_GEMINI_TRANSCRIBE_PROMPT` (`cloud_gateway.py:901`) primes for romanised Bengali by design, and a plain English question came back as *\"ki service\"* — answered correctly for the question it heard, which is worse than a failure because it looks like an answer. The audio path itself is proved: `brains.usage.audio` reached `gemini_ok` with no fallback on 2026-08-27.",
        "**14 — the situation on the persona envelope.** `where` already rides on it (`:4107`); place, battery and link as one field is the rest. The most character per line of code left anywhere in the plan.",
        "**12, the second half — retire the master token**, once `master_calls` has gone quiet. Not before, and the counter is the evidence rather than the calendar.",
        "**11 — nothing is owed here any more.** The listener was gated on the token split and the gateway half is live, so the gate is open; what remains is a service registration and a runtime permission, which is app work behind a native build."
      ]
    }
  },
  "goals": [
    {
      "id": "reachable",
      "title": "He reaches you without the app being open",
      "criteria": [
        3
      ],
      "unlocks": "The difference between an assistant and an app you remember to check. Push, both channels and the departure briefing are proved, and since 2026-08-26 the phone fallback arms itself, reads the registration back and names its own refusals. What is missing is continuous presence: the notification listener, which is gated behind the token split — and four checks with the phone in hand, because surviving a reboot turned out to be nothing to build and everything to verify."
    },
    {
      "id": "conversation",
      "title": "A conversation you can trust",
      "criteria": [
        1,
        2
      ],
      "unlocks": "Every state naming itself, and nothing arriving twice or lying once. The chat works end to end; the gap is that a wrong line is currently indistinguishable from a right one — a duplicate, a capitalised Sir, a weekday invented out of a stored pattern."
    },
    {
      "id": "voice",
      "title": "He speaks, and he listens",
      "criteria": [
        4
      ],
      "unlocks": "The oldest unverified thing here, and the largest single gap in the chat. Until both halves land it is a beautifully built text box, which is not what was asked for."
    },
    {
      "id": "situated",
      "title": "He knows where and when he is",
      "criteria": [
        5
      ],
      "unlocks": "One field on the persona envelope turns a generic answer into a situated one — place, battery, link. The roadmap calls it the most character per line of code available anywhere in the plan."
    },
    {
      "id": "memory",
      "title": "He remembers, and it survives a deploy",
      "criteria": [
        6
      ],
      "unlocks": "Today he learns four things and all of them are about the handset. Nothing you say is ever promoted to something he knows, and what the gateway does hold is wiped by the next deploy."
    },
    {
      "id": "acts",
      "title": "He acts on your behalf, with the brakes on",
      "criteria": [],
      "unlocks": "Approve-and-deny governance, the desk-watch countdown and opening an app by name are proved. What is missing is everything needing the gateway to hold state: scripts beyond listing them, run history, and rules you declare once."
    },
    {
      "id": "security",
      "title": "A compromised token does not expose a life",
      "criteria": [
        7
      ],
      "unlocks": "The gate in front of every new sense rather than a follow-up to them. One string still gates the socket, push, the commute route and app state alike, and nothing expires — so the notification listener cannot responsibly be built until this is."
    },
    {
      "id": "product",
      "title": "It behaves like a real product",
      "criteria": [
        8,
        9,
        10
      ],
      "unlocks": "A crash you can see, a second device that is possible, and an app usable by someone who cannot see it well. Shipping a fix within the hour is already solved; noticing that a fix is needed is not."
    }
  ],
  "criteria": [
    {
      "n": 1,
      "title": "Every state names itself",
      "status": "partial",
      "blockedBy": "brain",
      "detail": "Already the rule, and the two live defects are both what happens when it slips. A silent failure in a security path is a security failure. One defect is fixed in the app; the other is fixed in the brain and undeployed."
    },
    {
      "n": 2,
      "title": "No feature arrives twice or lies once",
      "status": "partial",
      "blockedBy": "brain",
      "detail": "The anti-duplicate gate is built and its decision carries tests; no departure window has run yet. The false Saturday remark is fixed in the brain as c86d176 and not deployed."
    },
    {
      "n": 3,
      "title": "He can be reached without the app being opened",
      "status": "partial",
      "blockedBy": "device",
      "detail": "Push is proved — it buzzed. `BOOT_COMPLETED` turned out to be granted already, measured on the installed APK rather than on the app half of the manifest, so reboot survival is a check rather than a build. The phone fallback was found unarmed on 2026-08-26 and now reads its registration back, records why it failed and repairs itself from Places. What is left in this repo is the notification listener, which is gated by criterion 7; everything else on this criterion is a phone in hand."
    },
    {
      "n": 4,
      "title": "He speaks and he listens",
      "status": "none",
      "blockedBy": "device",
      "detail": "The microphone has never been exercised — the oldest unverified thing here. Voice out is not built at all, and until both land this is a beautifully themed text box."
    },
    {
      "n": 5,
      "title": "He knows where and when he is",
      "status": "none",
      "blockedBy": "brain",
      "detail": "One field on the persona envelope, and it is a gateway change. The roadmap calls it the most character per line of code in the whole document."
    },
    {
      "n": 6,
      "title": "Memory survives a deploy",
      "status": "none",
      "blockedBy": "brain",
      "detail": "Rolling memory is in Postgres rather than RAM, but deploy-durable state is committed and undeployed. Render's disk goes on every deploy, and that has silently disarmed the briefing twice."
    },
    {
      "n": 7,
      "title": "A compromised token does not expose a life",
      "status": "none",
      "blockedBy": "brain",
      "detail": "One string still gates the socket, push, the commute route and app state alike, and nothing expires. A gate on the senses, not a follow-up: once data has been collected badly, no later fix un-collects it."
    },
    {
      "n": 8,
      "title": "A crash is visible",
      "status": "partial",
      "blockedBy": "app",
      "detail": "Shipping a fix within the hour is solved — the OTA channel is verified as of today. Seeing the crash at all is not: a crash is silent, and adb logcat on the one machine that built the APK is the only diagnosis."
    },
    {
      "n": 9,
      "title": "A second device is possible",
      "status": "none",
      "blockedBy": "app-build",
      "detail": "Signed with Expo's debug keystore, which is why a local APK and an EAS-signed one cannot replace each other. Fine for one phone, not for the second."
    },
    {
      "n": 10,
      "title": "It is usable by someone who cannot see it well",
      "status": "partial",
      "blockedBy": "device",
      "detail": "Contrast is measured and passing at 4.78 and 4.68 against a 4.5 bar; reduced motion already defaults from the OS. Nothing has been checked with a screen reader."
    }
  ],
  "areas": [
    {
      "name": "Transport, pairing, security",
      "rows": [
        {
          "id": "link-fallback",
          "name": "LAN probe → cloud fallback → WebSocket, reconnect",
          "status": "proved",
          "blockedBy": "none",
          "note": "One reducer serves every tab.",
          "goal": "conversation"
        },
        {
          "id": "one-socket",
          "name": "One socket per launch; chat survives force-stop",
          "status": "proved",
          "blockedBy": "none",
          "note": "",
          "goal": "conversation"
        },
        {
          "id": "pair-rotate",
          "name": "Pairing token in SecureStore, with rotation",
          "status": "proved",
          "blockedBy": "none",
          "note": "New accepted, old refused 403.",
          "goal": "security"
        },
        {
          "id": "biometric",
          "name": "Biometric lock, re-locking on background",
          "status": "proved",
          "blockedBy": "none",
          "note": "`strong` only — Android rejects `BIOMETRIC_WEAK | DEVICE_CREDENTIAL` outright, and with a passcode fallback enabled no sheet appears and the promise never settles.",
          "goal": "security"
        },
        {
          "id": "desk-key",
          "name": "Desk-key handshake (sealed turns)",
          "status": "partial",
          "blockedBy": "desk",
          "note": "**Cause found, fixed and PROVED ACROSS A REAL DEPLOY on 2026-08-29 — the sealed path itself is still owed.** `has_desk_key: false` was read off the LIVE gateway with the desk off, which is the ordinary state of this system: the PC being off is exactly why a fact is queued. The key lived in memory and in a file on Render's disk, so every deploy and every spin-down returned the gateway to the one state where `queue_fact` cannot seal and DROPS the turn. Eighteen went that way in a week. The desk's public half and the sealed queue now mirror into the same Postgres `gateway_state` table the schedule uses — injected as a hook, so `fact_outbox` keeps its stdlib-only import discipline, and what crosses is ciphertext and one public key. **Measured live:** on `445c3a9` the desk bridge was attached for twelve seconds and the cloud went `has_desk_key: false` → `true`; on the NEXT deploy (`fded484`, a new container, desk not connected) it read `true` again, out of Postgres. 44 checks in `test_fact_transport.py`. **Named gap:** no fact has yet travelled the whole path — queued while the desk was off, then drained into desk memory on connect. That needs one cloud-answered turn with the PC off, and then the desk on.",
          "goal": "security"
        },
        {
          "id": "bridge-secret",
          "name": "`BRIDGE_SECRET` rotation",
          "status": "partial",
          "blockedBy": "desk",
          "note": "**The blocker was ordering, and it is gone as of 2026-08-29; the rotation itself is still owed.** The gateway and the desk read this secret from two different places, so whichever moved first locked the other out — and the desk may be off when the change is made, which is why a known-leaked value stayed live for weeks. The gateway accepts `BRIDGE_SECRET_OLD` alongside `BRIDGE_SECRET` for one window now, logs loudly on every connect that still uses the old one, and reports `bridge_rotation: {old_accepted, connects_on_old}` on `/health` — so the old value can be deleted on evidence rather than on a guess. **His four steps are written out in `jarvis-backend/CLOUD_GATEWAY.md`:** set both and deploy, move the desk's `.env` when that machine is next on, watch the count reach zero, delete the old one. No window where either end cannot reach the other.",
          "goal": "security"
        },
        {
          "id": "token-split",
          "name": "Capability-split tokens",
          "status": "untested",
          "blockedBy": "brain",
          "note": "**WRITTEN 2026-08-29 on the home machine, both halves, and NOT LIVE.** The gateway derives a short-lived token per capability from `APP_TOKEN` (`POST /app-tokens`, master-only): `link`, `push`, `state`, `memory`, `say`. A token is `j1.<cap>.<exp>.<mac>` with the mac keyed by the master itself, so verification is stateless, **rotating the master revokes every derived token at once**, and a leaked `push` token buys the push route and nothing else — it cannot even mint itself a new one. This app exchanges once and stores the set beside the master (`src/link/capabilityTokens.ts`), attaches the right one per route, and re-mints three days before they lapse. **The master still opens every route on purpose**: this install presents it, and an auth change that locked him out of his own assistant would be worse than the leak it prevents — `/health` counts every master use per route so the migration is a number. 68 checks in `test_app_tokens.py`, 20 + 3 + 7 jest here. **Owed: a deploy and an OTA publish, then `master_calls` going quiet.**",
          "goal": "security"
        },
        {
          "id": "token-expiry",
          "name": "Token expiry",
          "status": "untested",
          "blockedBy": "brain",
          "note": "**Written 2026-08-29 with [token-split], and the same deploy is owed.** Every derived token carries an expiry (`APP_TOKEN_TTL_DAYS`, default 30, declared in `render.yaml`). An expired one is refused with `401 {\"error\": \"token_expired\"}` rather than a bare 401, because the difference decides what the phone does: this app mints again and retries **once**, and a second expiry after a fresh mint is a clock problem rather than something to loop on. A caller may ask for a shorter TTL and never a longer one. The master itself does not expire and is not meant to — it is the pairing secret in SecureStore, and rotating it is what revokes.",
          "goal": "security"
        }
      ]
    },
    {
      "name": "Talking to him",
      "rows": [
        {
          "id": "chat-text",
          "name": "Text chat, both directions",
          "status": "proved",
          "blockedBy": "none",
          "note": "",
          "goal": "conversation"
        },
        {
          "id": "chat-stream",
          "name": "Replies arrive word by word",
          "status": "proved",
          "blockedBy": "none",
          "note": "",
          "goal": "conversation"
        },
        {
          "id": "chat-markdown",
          "name": "Markdown rendered, not shown as asterisks",
          "status": "proved",
          "blockedBy": "none",
          "note": "",
          "goal": "conversation"
        },
        {
          "id": "chat-order",
          "name": "The chat log in the right order, once each",
          "status": "proved",
          "blockedBy": "none",
          "note": "**Proved on `84f40716`, 2026-08-27, on update `01a0429e`** — read top to bottom by scrolling the whole ~100-entry log, not from one screenshot, which is how this row was wrongly called done before. **Every timestamp ascends**, across four days and three day rules (`Friday`, `Tuesday`, `Yesterday`, `Today`), including the exact case reported broken: yesterday's 19:00 briefing sitting above today's 07:00.\n\nIt took two fixes today and the first was incomplete. `place()` orders what arrives; **`inOrder()` had to be added for what was already on disk**, because a relaunch restores the file as written and `place()` can never reach it — a fix that only guarded new entries would have left the phone looking identical and read as a third failure. That is `hydrate`'s own sentence about duplicates, applied to order.\n\n**The one same-text pair in the log is not a duplicate:** two identical turns 60 seconds apart, and the operator confirms he sent it twice by hand after the first appeared to get no reply. Sixty seconds is far outside the 5-second window and the code is right to treat it as a second send. **What made him resend is a separate defect, recorded under `chat-stuck-sending`.**",
          "goal": "conversation"
        },
        {
          "id": "chat-stuck-sending",
          "name": "A turn interrupted mid-send stops claiming to be sending",
          "status": "proved",
          "blockedBy": "none",
          "goal": "conversation",
          "note": "**Found and closed on `84f40716`, 2026-08-27.** A `thanks Jarvis` from Monday 20:02 had been reading `SENDING` for three days: `sending` is the gap between `local_command` and `turn_sent`/`turn_failed`, both of which belong to a running process, so killing the app in between leaves the turn written to disk mid-flight with nothing to move it. Its owner read that as never sent and re-sent it by hand — **the only duplicate in that log is this bug's footprint.**\n\n**Detected on restore, not by age.** A restored turn still saying `sending` has lost the process that owned the send, which is a fact; a threshold would be a guess about how long a send may take and would eventually accuse a slow one in the current session. Only the restored half is touched.\n\n**A new state rather than reusing `failed`, and that distinction is the point.** `failed` renders `NOT SENT` and carries this app's one unambiguous retry, justified by nothing having carried the message. An interrupted turn may already have gone — the window spans `link.send()` — so `NOT SENT` would be a guess and a safe-looking `SEND AGAIN` on *run script X* could run it twice. `awaiting` asserts *carried*, equally unknown. `INTERRUPTED` says what happened and claims nothing about the outcome: tone `bad`, no retry, words left there to be copied.\n\n**Seen on the phone at `01a042b3`:** the Monday turn now reads `You · 20:02 INTERRUPTED` in red. It repaired itself on load, since `hydrate` does the marking — no migration, and nothing else on the device had to change. 7 tests, 5 of which failed first."
        },
        {
          "id": "voice-rule-replies",
          "name": "The voice rule applied to what the model writes",
          "status": "broken",
          "blockedBy": "brain",
          "note": "**Found on the phone 2026-08-24.** `sir` is punctuation — lowercase, spent once. The situation line obeys it; every model reply capitalises it: *Standing by, Sir.*, *I can’t see your screen from here in the cloud, Sir.*, *I can’t authorise task approvals from the cloud, Sir.* Systematic rather than a one-off, and the same gap the nudge path has. The rule lives in `commute.ts` and `_briefing_text`; the persona prompt never got it. **Seen again 2026-09-01 on the current bundle**, in a reply about a photo: *\"…is a rather grim sight, Sir.\"* Capitalised, mid-sentence. So the gateway fix is still not deployed, and this row has fresh evidence rather than only the August sighting. Seen a second time the same afternoon, in the reply to the retried photo: *\"…scribbled notes, Sir.\"* Twice in one hour on the current bundle.",
          "goal": "conversation"
        },
        {
          "id": "unprompted-weekday",
          "name": "No unprompted weekday assertion",
          "status": "broken",
          "blockedBy": "brain",
          "note": "**Seen again on the phone 2026-08-24, a Monday.** *I can’t authorise task approvals from the cloud, Sir. Are you working this Saturday, by the way?* — a weekend question appended to an unrelated refusal. Same class as the false Saturday shift: a stored Mon–Fri pattern being asserted as a fact about today. The fix is committed in the brain as `c86d176` and undeployed, which is exactly what this looks like.",
          "goal": "conversation"
        },
        {
          "id": "no-reasoning",
          "name": "Reasoning monologues can never reach the screen",
          "status": "proved",
          "blockedBy": "none",
          "note": "`_strip_reasoning()`. **Device pass 2026-08-24:** two full screens of real model replies read off the phone, including multi-sentence answers and one that reasoned about a screenshot — no monologue, no stray tags, nothing leaked. Previously proved in the harness only.",
          "goal": "conversation"
        },
        {
          "id": "opening-line",
          "name": "The opening line is the real situation",
          "status": "proved",
          "blockedBy": "none",
          "note": "On-device, no model, no await. **Read again 2026-08-24:** *12:24 PM, sir. You are at Office and Office briefing at 7:00 PM.* Note the lowercase `sir` — this line obeys the voice rule, which is what makes the replies below it violating the same rule so visible.",
          "goal": "conversation"
        },
        {
          "id": "capabilities-answer",
          "name": "“What can you do”, answered without a round trip",
          "status": "proved",
          "blockedBy": "none",
          "note": "On-device, so it answers with everything even offline.",
          "goal": "conversation"
        },
        {
          "id": "capabilities-screen",
          "name": "A Capabilities screen listing the same thing",
          "status": "proved",
          "blockedBy": "none",
          "note": "One list, two surfaces. Read back over adb.",
          "goal": "conversation"
        },
        {
          "id": "status-panel",
          "name": "A status panel naming every seam",
          "status": "proved",
          "blockedBy": "none",
          "note": "Eight rows, four states, assembled on the device so it is readable with nothing connected at all — which is exactly when it will be looked at. **Read on the phone again 2026-08-24, on the new bundle:** all eight rows render, the caption counts `1 OFF` for the sleeping desk alone, and the briefing row reads `AT THE GATEWAY` while the link is cloud. Named gap: the third briefing state added that day, `CANNOT TELL`, cannot be reached on a cloud-linked phone — the stamp is fresh, so it would take two days of workspace-only sessions or a debug build to see it. **Reviewed 2026-08-27 and the gap is not work — it is a state that cannot be induced.** `CANNOT TELL` needs `cloudArmedState` to read `stale`, which needs the upload stamp older than `CLOUD_TTL_HOURS` (48) or dated in the future. Neither is reachable on demand: the stamp refreshes on every cloud connect, and the only lever that would fake it is moving the phone's clock — **which must not be done**, because `timeline` is three days into a four-day count and the journal is time-keyed, so a clock move would cost more than this row is worth. It is covered by test (`statusPanel.test.tsx:73`), and the eight rows and four states have been read on the device twice. **This is the same shape as `fallback-armed` and should be left `partial` for the same reason** rather than being quietly promoted or treated as an open task: the honest `partial`s here are ones nobody can close, not ones nobody has done. **PROVED 2026-09-01: the fourth state has now been seen.** The row argued that `CANNOT TELL` could not be induced, and it was right about the clock and wrong about the ceiling — the stamp itself can be written back past its window, which touches neither the clock, the schedule, nor the gateway's copy. `ageCloudStamp` does that from Places, and the panel then read **`CANNOT TELL`** with a grey dot and *\"Not uploaded in two days. The gateway may still hold it; the phone will brief as well.\"* **The caption still read `1 OFF`** — the sleeping desk alone — which is the design holding: unknown is not off, and that distinction was the whole argument for four states rather than two. Driven from the laptop, because the control is a `Pressable`. Second row today where \"cannot be induced\" turned out to mean \"nobody built the lever yet\".",
          "goal": "conversation"
        },
        {
          "id": "push-address",
          "name": "Whether the gateway holds a push address",
          "status": "proved",
          "blockedBy": "none",
          "note": "Nothing exposed this before, and it is the most diagnostic fact in the app.",
          "goal": "conversation"
        },
        {
          "id": "photo-preview",
          "name": "Photo preview and caption before sending",
          "status": "proved",
          "blockedBy": "none",
          "note": "",
          "goal": "conversation"
        },
        {
          "id": "photo-marker",
          "name": "A photo answering with its own lookup marker",
          "status": "proved",
          "blockedBy": "none",
          "note": "Shared by the text and vision paths.",
          "goal": "conversation"
        },
        {
          "id": "photo-thumb",
          "name": "A thumbnail in the chat instead of the word “Photo”",
          "status": "proved",
          "blockedBy": "none",
          "note": "**Already built, and the row was wrong.** `sendPhoto` carries the shrunk copy `uri` on the turn and `SentPhoto` renders it above the caption, with an `onError` fallback for a cache Android has cleared. Found on 2026-08-31 by reading the code the row said did not exist. Never deliberately looked at on the phone, so `untested` rather than `proved`. **PROVED on the phone 2026-09-01, 14:46.** A photo sent from the composer renders in the bubble at its full width with the marker beneath it, under a `Today` rule. The camera control is a `Touchable`, so `adb shell input` cannot reach it and the send needed a finger — the third time that has decided how a check gets done.",
          "goal": "conversation"
        },
        {
          "id": "photo-inflight",
          "name": "Photo-in-flight indicator; a failed send stays recoverable",
          "status": "proved",
          "blockedBy": "none",
          "note": "**Recovery built 2026-08-31; the in-flight half was already there.** A photo turn has carried `SENDING` / `NOT SENT` since the marks shipped, because `sendPhoto` settles its own turn. What was missing was the recovery the row names: the send handed base64 to the socket and kept only a `uri` in the log, so a failed photo had a picture on screen and no bytes to re-send. `reshoot` rebuilds them through the same shrink the first send used — a retry that skipped it would put a full-size frame on a socket sized for 200 KB — and the caption comes back off the turn through `captionOf`, one place owning the marker so a retry cannot send the word \"Photo\" as though it had been typed. **A picture the cache has lost says so** rather than retrying forever: `resendPhoto` answers false and the chat says the photo is no longer on the phone. 11 tests. Unproved on the device. **The in-flight half is PROVED on the phone 2026-09-01:** the turn settled rather than sitting at `SENDING`, and the answer came back inside a minute — *\"An empty cup at nearly three in the afternoon is a rather grim sight, Sir.\"* — so the vision path is end to end as well. **The named gap is the recovery**, which is the half built on 08-31 and still never exercised: it wants a send that fails with the network off, and then `RETRY`. Both the camera and the retry control are `Touchable`, so that check cannot be driven from the laptop. **The recovery is PROVED on the phone, 2026-09-01, 14:59.** Airplane mode on, a photo sent, and the turn marked `NOT SENT` with `SEND AGAIN` beside it and the picture still rendered — a failure that stays recoverable rather than a bubble that sits at `SENDING`. Network back, `SEND AGAIN` tapped: the failed turn was withdrawn and re-sent, and **the reply is about the photograph** — *\"A capped pen and some scribbled notes, Sir…\"* — so `reshoot` rebuilt the bytes from the cached uri and they reached the vision model. Before 08-31 that was impossible: the base64 went to the socket and only a local uri stayed in the log, so a retry could only have sent the caption as text. **Both controls are `Touchable`, so the whole check needed a finger** — adb could read it and could not drive it.",
          "goal": "conversation"
        },
        {
          "id": "ticks",
          "name": "Sent / delivered / read ticks",
          "status": "none",
          "blockedBy": "brain",
          "note": "Only the gateway can say delivered and read, and it says neither. Needs a per-message id on both sides. The largest fully-specified thing left.",
          "goal": "conversation"
        },
        {
          "id": "reply-to",
          "name": "Reply to a message",
          "status": "none",
          "blockedBy": "brain",
          "note": "Wants the same per-message id as the ticks, so it follows them rather than inventing a second identity for a message.",
          "goal": "conversation"
        },
        {
          "id": "mic-in",
          "name": "Microphone in",
          "status": "partial",
          "blockedBy": "brain",
          "note": "**Run at last on 2026-08-27, and the transport passed on the first try.** `brains.usage.audio` went `{gemini_ok: 0, fell_back: 0}` to `{gemini_ok: 1, fell_back: 0, last_error: null}` — the clip reached the gateway, Gemini accepted it, and nothing fell back to Groq. **The mime fear this row was written around was already answered in code:** `_AUDIO_MIME` maps `m4a` to `audio/aac`, which is exactly what Google documents, so there was never anything to fix. Note the counter is incremented on a call that did not throw, so it proves the format was accepted and not that words came back — the chat is what settles that.\n\n**The named gap is the transcript.** *\"hi jarvis\"* came back as **\"ki service\"**, and the reply — correct Benglish for the question it thought it had been asked, about what the cloud can do without the desk — was right for a transcript that was wrong. The cause is visible in `_GEMINI_TRANSCRIBE_PROMPT`, which primes hard for romanised Bengali (*\"the speaker mixes Bengali and English… anything that sounds like Hindi is Bengali\"*). That prompt exists to fix the opposite failure and it works; on a two-word English greeting it overcorrects, and `ki` is a real Bengali word. **One sample, and a poor one** — two words, one of them a proper noun the prompt never names. Cheapest thing to try is telling the prompt the assistant is called J.A.R.V.I.S., which is a gateway change and so blocked with the rest.\n\n**2026-08-27, second clip: voice is cleared of the wrong answer it appeared to give.** A spoken *\"how far is home from here\"* was answered with an invented distance, which looked like the transcript being mangled again. **Typing the identical question reproduced it exactly** — so the fault is `_FAR_RE`, recorded under `weather-distance`, and had nothing to do with the microphone. `gemini_ok` reached 2 with `fell_back` still 0. **A separate defect did show up on that clip, and it is now diagnosed:** the transcript never rendered as a user turn, where the first clip's did. **The socket died while Gemini was transcribing, and only the answer has a lifeboat.** `emit()` returns False when the socket is gone; `deliver()` checks that and pushes the answer instead (`cloud_gateway.py:3977`), while the transcript at `:4095` throws the result away and has no push path at all. Confirmed on the device: `numPostedByApp` went 2 to 3, so the reply arrived by push — which is why the chat shows an answer with no question above it. The window is the app being backgrounded mid-turn, where `LinkMachine.suspend` closes the socket deliberately; the first clip survived it by being short. **The codebase already learned this and half-applied it** — `emit`'s own docstring says False was once indistinguishable from success, *\"which is how a finished answer came to vanish silently\"*. The transcript was never given the same protection. Fixing it wants the push payload to carry the transcript so the app can write the user turn: prepending it to the answer is not open, since the code is explicit that a transcript sent as a status message is a lie about who spoke.",
          "goal": "voice"
        },
        {
          "id": "voice-out",
          "name": "Voice out",
          "status": "none",
          "blockedBy": "app",
          "note": "The largest single gap in the chat, and app-only — `expo-speech`. **Voice IN is no longer the unknown half, as of 2026-08-27:** the microphone path works end to end and only the transcript is unreliable. This row is unchanged — nothing speaks yet.",
          "goal": "voice"
        }
      ]
    },
    {
      "name": "Notifications and being spoken to",
      "rows": [
        {
          "id": "task-heartbeat",
          "name": "Whether the background task is running, not merely registered",
          "status": "proved",
          "blockedBy": "none",
          "goal": "reachable",
          "note": "**Seen on `84f40716`, 2026-08-26, on update `01a03dfe`.** The row read *“Last ran 4 minutes ago. Nothing was due. 1 run recorded.”* against a run the log timestamps at 17:51:39 — so the stamp the task writes is the stamp the screen reads. `getStatusAsync()` answers a different question and reported `Available` throughout the week this feature had not run. `healthFrom` turns registration, availability, the stamp and the last arming attempt into one of six readings; `unarmed` further splits by that attempt — never asked, refused with a reason, or armed and since dropped. 32 tests."
        },
        {
          "id": "push-sleeping",
          "name": "Push to a sleeping phone",
          "status": "proved",
          "blockedBy": "none",
          "note": "It buzzed. Reaches a dead app and is exempt from the background-execution block.",
          "goal": "reachable"
        },
        {
          "id": "channels-exist",
          "name": "Both notification channels exist on the device",
          "status": "proved",
          "blockedBy": "none",
          "note": "Confirmed by adb, neither deleted.",
          "goal": "reachable"
        },
        {
          "id": "channel-rank",
          "name": "The desk alert outranks an everyday one",
          "status": "proved",
          "blockedBy": "none",
          "note": "Importance 5 against 3.",
          "goal": "reachable"
        },
        {
          "id": "channels-cleaned",
          "name": "Renamed channels are deleted rather than left behind",
          "status": "proved",
          "blockedBy": "none",
          "note": "Six old channels all confirmed deleted. This app has renamed its everyday channel eight times, and every rename silently broke replies until the gateway was told.",
          "goal": "reachable"
        },
        {
          "id": "tap-opens-chat",
          "name": "A notification tap opens Chat",
          "status": "proved",
          "blockedBy": "none",
          "note": "",
          "goal": "reachable"
        },
        {
          "id": "deskwatch-closed",
          "name": "A desk-watch alert reaching a closed app",
          "status": "proved",
          "blockedBy": "none",
          "note": "Tapping it opens the alert screen.",
          "goal": "reachable"
        },
        {
          "id": "briefing-push",
          "name": "A pushed departure briefing, unprompted",
          "status": "proved",
          "blockedBy": "none",
          "note": "It arrives. It also arrived twice — the gate is built but no departure window has run since.",
          "goal": "reachable"
        },
        {
          "id": "fallback-armed",
          "name": "The phone fallback is actually armed, and says so when it is not",
          "status": "proved",
          "blockedBy": "none",
          "goal": "reachable",
          "note": "**Found unarmed on 2026-08-26, fixed, and the armed half proved on the device the same day.** `setCommuteTask` reads the registration back after asking, because `registerTaskAsync` resolving proves only that it did not throw; the result carries the platform’s words, every attempt is recorded for a screen to read after a relaunch, and `App.tsx` logs a failure rather than discarding it. **Proved:** WorkManager holds job `#u0a495/288`, the task executes and writes its stamp, and Places reports it truthfully. **The named gap is the other half — “says so when it is not”.** The phone re-arms itself at launch and on every visit to Places, so the unarmed state cannot be induced on demand; it is covered by tests and by the reading that named it, not by a sighting. **2026-08-27 did not move this and is worth recording as such.** The 07:00 window was the first real one with the phone armed underneath a live gateway, and the phone correctly stayed silent — so the fallback still has not been seen to POST anything. Standing down is the gate’s proof, not this row’s. **2026-08-31: the missing half is now checkable in one tap.** The reason it stayed `partial` was never doubt about the code — it was that the app re-arms at launch and on every visit to Places, so the unarmed state healed faster than anyone could look at it. Places now offers `TEST` beside the row, which unregisters the task and re-reads the health so the sentence can be read for real; the stored schedule is untouched, and leaving Places and returning re-arms it by the same path that fixed the real occurrence on 2026-08-26. **This row is now one device check away from proved, and that check is thirty seconds.** **PROVED on the device 2026-08-31, 18:47, both halves and the repair.** Driven from the laptop over wireless debugging. Armed beforehand: `JOB #u0a495/629` held by WorkManager. `TEST` pressed by `adb shell input tap` — which is the reason it is a `Pressable` and not a `Touchable`, since the latter refuses synthetic taps. The row then read *\"Switched on, and Android holds no registration for it. The fallback is not armed. It was armed 1 minute ago and Android has since dropped it.\"* and `dumpsys jobscheduler` agreed — **no JOB entry for u0a495 at all**, so the screen and the platform were checked against each other rather than the screen being taken at its word. `TEST` also correctly disappeared once there was nothing left to disarm. Leaving Places and returning brought back `JOB #u0a495/630` — a new id, so the repair is a real re-registration and not a stale read. The one thing this row could never show has now been seen."
        },
        {
          "id": "briefing-content",
          "name": "Briefing content, thresholds, quiet-day announcement",
          "status": "proved",
          "blockedBy": "none",
          "note": "Thresholds on real figures, never the model. Quiet day announced with its figures rather than silently, after silence was read as breakage for four days. **The wording rotates as of 2026-08-26** — a persisted cursor per slot in `briefingVoice.ts` spends a pool of 6–7 remarks before any line returns, and the titles rotate with it. The figures deliberately do NOT vary: a measurement rephrased for novelty is one you can no longer compare with yesterday. Every variant keeps the actionable word, so Android truncating the shade cannot eat the instruction. 30 tests, and the rules are asserted over the whole table rather than over one rendering. **Only the phone-sent briefing is affected;** when the gateway is armed it writes its own text.",
          "goal": "reachable"
        },
        {
          "id": "gateway-briefing-wording",
          "name": "Rotating wording in the gateway-sent briefing",
          "status": "proved",
          "blockedBy": "none",
          "note": "The phone rotates its own wording; the gateway does not. When `cloudArmed` is true the phone stays silent by design and the gateway posts `_briefing_text`, which is a fixed template — so on a cloud-linked phone the repetition the rotation was built to fix is still what arrives. Same shape as `briefingVoice.ts`: a pool per slot and a cursor that survives a deploy, which is why it wants `fix/durable-state` merged first rather than a second store that a redeploy wipes. **Seen rather than reasoned about, 2026-08-27.** The Office briefing at 17:59 on the 26th and the Home briefing at 07:00 on the 27th carry the identical remark — *\"An umbrella, unless you’ve grown fond of arriving wet.\"* — and differ only in their figures, 51% against 65% with a storm line added. Two mornings, one sentence. It also cost a diagnosis: the changed figures read as rotated wording from the phone, and the gateway was briefly suspected of having gone silent. **2026-08-31: the rotation is written and deployed.** `02604c6` on `feat/cloud-gateway` ports this repo's `briefingVoice.ts` line for line rather than inventing a second voice, and `fix/durable-state` is merged, so the cursor survives a redeploy — which was the reason it had to wait. **Owed: two gateway briefings on different days, compared.** The pair that proved the defect were word-for-word identical, so word-for-word difference is the check. **PROVED 2026-09-01, on the same slot two days running, which is the comparison that settles it.** Home 07:00 on 08-31: *\"Before you leave Home, sir — A 60% chance of rain on your way out, around 0.5 mm. An umbrella, unless you have grown fond of arriving wet. (7 AM–10 AM)\"*. Home 07:01 on 09-01: *\"Home, and one thing to take with you, sir — A 94% chance of rain on your way out, around 1.2 mm. An umbrella would be the sensible half of this conversation. (7 AM–10 AM)\"*. **Title and remark both moved; the figures moved independently of them; and `umbrella` — the actionable word — survived in both**, which is the rule that mattered because Android truncates the body in the shade. The evening slot the same day was different again (*\"A word before you leave Office, sir… An umbrella. It is by the door, where it has been all week.\"*). **Two slots on one day would NOT have proved this** — separate per-slot templates would look identical to rotation — which is why the same-slot pair is the evidence and was waited for. Read off screenshots the notifications had already been swiped from, plus the chat log, which keeps its own copy.",
          "goal": "reachable"
        },
        {
          "id": "speaks-first",
          "name": "He speaks first, once a day",
          "status": "untested",
          "blockedBy": "brain",
          "note": "Fired for the first time and was wrong: a bare substring match let a Mon–Fri pattern assert a Saturday shift, and the prompt then asserted it as true today. Fixed in the brain as c86d176, not deployed. The same body also capitalised `Sir`, which the voice rule forbids. **2026-08-31: the fix is deployed.** `c86d176` is contained in `origin/feat/cloud-gateway`, which is the branch Render watches — verified from this repo with `git branch -r --contains`, which is as much as this laptop can check. So the substring bug and the capitalised `Sir` are both live. **What is owed is a morning:** he speaks once a day at most, so this is proved by looking rather than by triggering.",
          "goal": "reachable"
        },
        {
          "id": "briefings-visible",
          "name": "Briefings visible in the Activity panel",
          "status": "proved",
          "blockedBy": "none",
          "note": "They had been filtered out entirely.",
          "goal": "reachable"
        },
        {
          "id": "read-unread",
          "name": "Read / unread per entry, surviving a restart",
          "status": "proved",
          "blockedBy": "none",
          "note": "**Proved, and then found half-wired on 2026-08-27.** Marks are per entry and survive a restart. What they did NOT survive was reading the chat: there are two unread systems — `readAt`, the timestamp behind Home’s *\"N new replies\"*, and `readIds`, the persisted set behind the bell — and only the Activity panel ever wrote to the second. So reading the conversation cleared its own marker and left the bell counting the very turns it had shown. Fixed at `28d682d`, published as `01a0422d`, **and confirmed gone on the phone the same afternoon.** The same fix closes the case one step earlier: a reply landing while the chat is open is marked read as it arrives, where before the tab bar counted an answer being read on screen.",
          "goal": "reachable"
        },
        {
          "id": "double-post-gate",
          "name": "The local task gated so it cannot double-post",
          "status": "proved",
          "blockedBy": "none",
          "note": "**Proved on `84f40716`, 2026-08-26 at 18:31:15.** A run forced inside the phone own departure window (18:30–19:00 for a 19:00 departure) while Home read `AT THE GATEWAY`: it exited in 260ms without fetching a forecast and posted nothing. The shade held exactly one briefing all evening — the gateway push at 17:59:18 — where 2026-08-21 saw both senders fire and the same briefing arrive twice. Forced rather than waited for, which settles the gate decision and not the scheduling; scheduling is proved separately by the unattended run at 18:07. **Held in a REAL window on 2026-08-27.** The 07:00 Home departure, with the phone rebooted the night before and the app never opened: exactly one notification arrived, and it was the gateway's. That is the same decision the 18:31 run settled by force, taken this time by a headless run nobody was watching.",
          "goal": "reachable"
        },
        {
          "id": "activity-detail",
          "name": "Full message on tap, day rules, paged list",
          "status": "proved",
          "blockedBy": "none",
          "note": "**Proved on `84f40716`, 2026-08-27**, on a timeline finally long enough to exercise every part of the row at once. The full message on tap: the 07:00 briefing whole, in a box that no longer collapses to a line — the defect this row was left `partial` for. The `Yesterday` rule separating two days, which had never been seen. And the paging: `SEE n MORE` revealed twelve at a time (`PAGE = 12`, `ActivityScreen.tsx:27`), the count fell by twelve a tap, the last page held four, and the button then removed itself rather than sitting there offering nothing. `TESTING.md` 3.2, 3.9 and 3.10.",
          "goal": "reachable"
        }
      ]
    },
    {
      "name": "Memory and the journal",
      "rows": [
        {
          "id": "facts",
          "name": "Facts stored and recalled",
          "status": "partial",
          "blockedBy": "brain",
          "note": "14 facts known. Volunteered exactly once, wrongly. **Read on the device 2026-08-27, all 17, and the store is in better shape than that sentence suggests** — parents, brother, employer and its address, both dogs with sex and dates, the PIN code, the marriage year. A photo of a dog he had never sent before was answered *\"That's Puku, Sir. He passed away on the fifth of August last year\"*, which matches the stored `died on 5 August 2025` exactly, and the disambiguation is real work rather than luck: **two dogs are stored**, and *\"who he **was**\"* (past tense) plus *male* selects Puku over Kitty, who is female and alive.\n\n**One row on that screen is a bug, and it caused today's worst answer.** `Kaustav is **currently** in Ichapur, West Bengal, India` — a time-sensitive claim in a permanent store. When `_FAR_RE` extracted `dest=\"here\"` and injected no route fact, the model reached for that, and with `Kaustav lives in Ichapur` beside it produced *\"since you're currently in Ichapur, sir, you've already arrived at home\"* while the phone's own header read `Office`. **So that answer was not invented from weights — it was a stale fact filling a hole the regex left**, which is worse, because it will happen again wherever a live lookup fails quietly. Every other fact on the screen is durable; this one is a snapshot wearing a fact's clothes, and the `where` block already carries live location on every single turn, so it is redundant as well as harmful.\n\nAlso visible: `Kaustav asked about Marco Polo`, which is not a fact about him at all. Extraction is storing conversation trivia beside load-bearing records.",
          "goal": "memory"
        },
        {
          "id": "facts-from-talking",
          "name": "Anything he learns from what you actually say",
          "status": "none",
          "blockedBy": "app",
          "note": "**The largest gap in the memory story, and it was not in this ledger until 2026-08-24.** `shareFacts` derives from the journal rollup and named places only — four keys, all about the handset: `phone:screen-time`, `phone:pickups`, `phone:top-apps`, plus the places. **Nothing reads the conversation.** How he works, who matters to him, what he cares about: durable only if typed by hand into the Memory screen. So a chat scrolls past `CHAT_CAP` (100 entries, roughly a day at real pace) and nothing was taken from it. The raw turns survive brain-side in `chat_turns`, so this is not data loss — it is that no turn is ever promoted to a fact.",
          "goal": "memory"
        },
        {
          "id": "chat-window",
          "name": "A chat log that is not a one-day window",
          "status": "partial",
          "blockedBy": "app",
          "note": "`CHAT_CAP = 100` in `hudReducer.ts`, original and deliberate — a phone should not carry an unbounded log. Measured 2026-08-24: the whole persisted log spanned **Fri 20:03 to Sat 15:28**, so at real conversation pace 100 entries is about one day. Worth naming because the cap is silent: nothing tells you a turn is about to leave, and nothing is harvested before it does — see [facts-from-talking]. The brain keeps the turns; only the phone forgets.",
          "goal": "memory"
        },
        {
          "id": "one-assistant",
          "name": "One assistant across desk, phone and chat",
          "status": "partial",
          "blockedBy": "brain",
          "note": "App and chat share one history. A desk answering with its own brain bypasses it, so those turns never join the shared history.",
          "goal": "memory"
        },
        {
          "id": "rolling-memory",
          "name": "Rolling memory durable across restarts",
          "status": "partial",
          "blockedBy": "brain",
          "note": "In Postgres, not RAM — the RAM claim was stale. Whether the shared-memory flag is actually on in Render's environment is unverified.",
          "goal": "memory"
        },
        {
          "id": "durable-state",
          "name": "Deploy-durable gateway state",
          "status": "untested",
          "blockedBy": "brain",
          "note": "Committed on `fix/durable-state`, two commits ahead of the gateway branch, undeployed. Until it merges, every deploy silently disarms the briefing.",
          "goal": "memory"
        },
        {
          "id": "chat-history",
          "name": "Chat history sent with the ask",
          "status": "none",
          "blockedBy": "brain",
          "note": "The envelope carries one turn.",
          "goal": "memory"
        },
        {
          "id": "journal",
          "name": "Journal: usage source, rollup, fact sharing",
          "status": "proved",
          "blockedBy": "none",
          "note": "Derived facts only, never rows — the pattern every later sense copies.",
          "goal": "memory"
        },
        {
          "id": "journal-denial",
          "name": "The journal’s denial path",
          "status": "proved",
          "blockedBy": "none",
          "note": "Reads “I cannot see your usage, sir — the permission is off.” Collected history stays visible under HELD ON THIS DEVICE, which is the honest distinction.",
          "goal": "memory"
        },
        {
          "id": "background-work",
          "name": "Any background work running unattended",
          "status": "proved",
          "blockedBy": "none",
          "note": "**Proved on `84f40716`, 2026-08-26 at 18:07.** The task ran with the app backgrounded and the phone disconnected from adb — nobody started it, and the count on Places moved from 1 to 2 with the stamp reading `Nothing was due`. Getting there needed the throttling lifted first: standby bucket **40 (RARE)** to **5 (EXEMPTED)** and the app added to the device-idle whitelist, after which `WITHIN_QUOTA` went from unsatisfied to satisfied and only the 15-minute timer remained. That is what the Battery restrictions row asks a person to do in Settings. Before that, measured the same day: roughly one job window a day and `Network: blocked=REASON_APP_STANDBY`.",
          "goal": "reachable"
        },
        {
          "id": "commute-task-tested",
          "name": "The commute task body exercised by a test",
          "status": "proved",
          "blockedBy": "none",
          "note": "10 tests, with the task callback captured at import and invoked against the real module. The gate cases were confirmed by removing the gate.",
          "goal": "reachable"
        },
        {
          "id": "timeline",
          "name": "Location timeline",
          "status": "proved",
          "blockedBy": "none",
          "note": "Sightings at named places, median last-seen per day, its own 28-day store. Silent for its first four days, by design. **Device pass 2026-08-24:** it is accumulating — the panel reads *Learning your hours at Office* and its countdown had moved from `4 MORE DAYS` to `3 MORE DAYS` since 08-21, which only happens if sightings are being recorded at a named place. Named gap: nothing it has learned has been *used* for anything yet, so its output is still unseen. **The named gap is addressed in code, 2026-08-27.** The app was spending four days learning an hour and then telling nobody: the figure surfaced in exactly one place, the anticipation remark, which fires ONLY when you are 45 minutes past it. So on every ordinary day it had learned something and kept it — failing the anticipation doctrine's own third test, *a figure you could disagree with rather than an adjective*. The WATCHING row now says the hour itself (`6:40 PM`) with a note naming the place and the days behind it, instead of `Office, ready`. `null` stays a real answer: enough days with no median still reads `ready` and invents no time. **Unproved until the fourth day lands** — the row reads `1 MORE DAY` tonight, so opening the app at Office tomorrow is what makes it speak. 4 tests. **PROVED 2026-09-01: the fourth day landed and the row speaks.** The WATCHING panel reads *\"When you are usually gone — 3:40 PM\"* with *\"Learned from your last 6 days at Office\"* beneath it, where it read `1 MORE DAY` on 08-27. So the median exists, it is surfaced where it can be disagreed with rather than only inside a remark that fires 45 minutes late, and the day count is named. The figure carries the app-open bias this store has always had — it is the median of last SIGHTINGS, not of leavings — which is why the panel quotes the days behind it.",
          "goal": "situated"
        },
        {
          "id": "call-log",
          "name": "Call log, archive import",
          "status": "none",
          "blockedBy": "app-build",
          "note": "Native build, and fatal for a store listing.",
          "goal": "memory"
        }
      ]
    },
    {
      "name": "Knowing and acting",
      "rows": [
        {
          "id": "places",
          "name": "Named places, location sharing, located answers",
          "status": "partial",
          "blockedBy": "none",
          "note": "**Proved on `84f40716`, 2026-08-27, from the Office**, which is the first time this row has been exercised since the fix that touched it. Three things at once. Location sharing on and the header naming `Office` — the label set by standing there, not a geocoder’s guess. *\"how far to home\"* answered **39.3 km by road, about 38 minutes** — so `home` resolved to the place he named, and the route was measured FROM where he was standing rather than from a district. *\"is it raining here\"* answered *\"currently clear at the office, sir, with no rain falling\"* plus a **95%** chance later — figures, and the place named. Asked from the Office deliberately: *\"how far to the office\"* would have answered zero and proved nothing. `TESTING.md` 7.1 and 7.2. **Found wrong on the phone, 2026-09-01, and fixed the same day.** Home and a named area about 150 m apart: walking to the area never changed what the app said. Two causes, and the radius was the smaller one. The fix was taken at `Accuracy.Balanced` — roughly a hundred metres, derived from wifi and cell rather than GPS — which was right while it only answered \"what is the weather here\" and wrong once the same reading decided WHICH NAMED PLACE he was in: wifi positioning anchors to routers it knows, so from down the road it kept returning his own living room. Now `Accuracy.High`, the match radius is 120 m rather than 250, and **the fix carries its own error so a place must win by more than it** — with two places 150 m apart and a reading good to 200 m, naming either is a coin toss reported as a fact. It answers null instead. **`partial` until the walk is done at home**: the office was never ambiguous — its nearest named neighbour is 700 m away — so only home can prove the repair.",
          "goal": "situated"
        },
        {
          "id": "background-sightings",
          "name": "Arrivals and departures seen with the app closed",
          "status": "untested",
          "blockedBy": "none",
          "note": "**Built and installed 2026-09-01, on the build that carries `ACCESS_BACKGROUND_LOCATION`.** Every timing this app has ever reported came from a sighting written when somebody happened to open it, and 2026-09-01 spent the day paying for that: an office he leaves at seven reported as gone by 3:40 PM, an arrival called early for a man who had slept there, and a repair (8:04 PM) that could only ever be an upper bound. One root, three wrong figures, each caught by the person they were about.  Android reports a geofence crossing with the app closed, two to five minutes late rather than hours, which is the only way a departure is ever measured rather than inferred. `noteSeen` now records `via: enter | exit`, and `leftBy` prefers a real exit and says `measured: false` when it is falling back to the old guess.  Asked for directly: *\"the app should learn it itself, Google Maps Timeline has all the data\"*. It does, and there is no route to it — Timeline moved on-device, no API exposes it, and a Takeout export is stale the moment it is made. Maps knows because it holds background location. This is the same permission doing the same job, for one app's own named places.  **Registered on the phone, 2026-09-01 18:14, on the APK built and installed the same hour.** Permission granted, WATCH pressed from the laptop, and the row went from *\"the permission is there and nothing is registered\"* to **\"Android is watching 10 places and will report you arriving or leaving within a few minutes, with the app closed\"**. Ten places, well inside the platform limit of a hundred.\n\n`dumpsys location` reports *Geofence Manager: service: unregistered* and that is NOT a contradiction: expo-location registers through Play Services, not the framework manager, so the framework dump is the wrong place to look. Do not read it as a failure.\n\n**It survives the process dying, which is the half that could be proved from a laptop.** `am force-stop` then a cold launch, 18:21: the row still read *\"Android is watching 10 places\"* with STOP beside it. Android holds the registration itself, so nothing has to re-arm it at launch the way the briefing task does — which also means `stopWatchingPlaces` is the only way back, and why the STOP control exists at all.\n\n**Still `untested` until an exit is seen.** The remaining claim is that a crossing is reported with the app closed, and no laptop can manufacture one. The control is on Places (WATCH), the radius is 120 m, and the first real proof is a departure whose recorded time matches the clock — which cannot be made to happen from a laptop, only by leaving.\n\n**The platform fired ten false departures the first evening, and that is now the interesting part of this row.** At 18:31, from the office, the phone posted *\"Left Office\"*, *\"Left Home\"*, *\"Left Musalman Para\"* and seven more, all in the same minute, and wrote a sighting for each. Play Services re-evaluates every region when the app process restarts and reports an exit for **each region the phone is outside of** — so standing at the office produces a departure from every other place he owns. Every event is real. Not one is a departure. Caught by him within a minute: *\"but i'm in office now, then why home?\"*\n\nThree rules came out of it, each with the failure it answers:\n\n1. **A person leaves one place at a time.** Two places reporting a departure inside ninety seconds is the platform, so the burst is dropped — and because the first exit of a burst cannot be told apart from a real one when it arrives, the repair reaches backwards and removes what was already written.\n2. **Wait before speaking.** Retracting was tried first, and it left *\"Left Home — 6:40 PM\"* standing in the shade about a house he was nowhere near: the dismissal is best-effort, the notification shade is not. The word now waits ten seconds and goes out only if the sighting survived. The sighting itself is never delayed.\n3. **You can only leave where you were.** A lone false exit has no burst to give it away, so it is judged by what came immediately before it: a departure from somewhere the app was not just seeing you is geometry, not a person. An exit with nothing before it still stands, because silence is not evidence.\n\n`pruneSweepExits` applies all of it at every launch, to the records already written and to anything a process that dies mid-sweep leaves behind.",
          "goal": "situated"
        },
        {
          "id": "leaving-notification",
          "name": "He tells you when you left",
          "status": "untested",
          "blockedBy": "none",
          "note": "**Asked for on 2026-09-01, in this shape: exits only, with a cooldown.** Ten named places reporting both crossings is a phone that buzzes all day about things you already know; the departure is the one carrying a figure the app could never measure before.\n\n*\"Left Office — 7:05 PM. Noted, sir.\"*, on the general channel, two to five minutes after the boundary is crossed and ten seconds after that. Forty-five minutes of quiet per place afterwards, per place rather than per phone, because leaving home and reaching the office are fifteen minutes apart on one morning.\n\n**TEST on the Places row posts the same notification** without writing a sighting or spending the cooldown — proved on the device at 18:31, which is how the sweep was discovered in the first place. This app has shipped notifications before that turned out silent, on the wrong channel, or cut off in the shade, so a preview is not a luxury.\n\n**`untested` until a real one arrives.** The first is expected leaving the office at 7:00 or 7:10 PM, and the figure it reports is also what closes [[background-sightings]].",
          "goal": "situated"
        },
        {
          "id": "weather-distance",
          "name": "Weather and distance from measured figures",
          "status": "proved",
          "blockedBy": "none",
          "note": "The search provider is unverified, and a silent search failure looks exactly like hallucination. Routing is a public server that knows the road graph and not the road, so durations are free-flowing and the context says so. **2026-08-27, from the Office:** both lookups answered with real figures — 39.3 km / 38 minutes by road, and a 95% precipitation chance against a clear sky now. So routing and forecast are reaching a server and returning measurements, which is the half that could be checked from the phone. `/health` reports `search: tavily`. **The named gap is unchanged:** that a TAVILY search answers is still unverified, and a silent search failure still looks exactly like a confident answer — the two lookups above are the route and forecast paths, not the search path. **BROKEN, found 2026-08-27, and it is one regex.** `_FAR_RE` (`cloud_gateway.py:2757`) accepts `to|from|until` before the destination, so *\"how far is home **from here**\"* extracts `dest=\"here\"`. Nothing matches a known place called \"here\", geocoding it returns junk, and **no route fact reaches the model** — which then answers from its weights and stored facts. Seen twice within fifteen minutes: *\"you're currently in Ichapur, sir, you've already arrived at home\"*, then *\"approximately 23 kilometers from the office to Ichapur — a local train from Bidhannagar Road should get you there in about 30 minutes\"*, both invented, both confident. The same question phrased *\"how far **to** home\"* answered 39.3 km correctly at 12:41, which is why this survived: **the feature works on the phrasing the test script uses and fails on the phrasing a person uses.** This is the exact shape this row was already warning about for search — a silent lookup failure is indistinguishable from a confident answer. Gateway-side, so it waits with the rest. **The distance half is FIXED and proved from the phone, 2026-09-01, 15:37.** Asked from the Office in the phrasing that broke it — *\"how far is home from here\"* — and answered *\"Home is 40.2 km by road from here, Sir. It'll take about 37 minutes to drive, assuming the traffic behaves.\"* Measured, and it agrees with the 39.3 km that the *\"how far to home\"* phrasing returned on 08-27, so the two phrasings finally give the same answer instead of one inventing a local train. The gateway fix is `efd1af1`, written on the home machine from this repo's handoff page. **`partial`, not `proved`, and the remaining gap is the one this row named first:** that a TAVILY *search* answers is still unverified, and a silent search failure still looks exactly like a confident answer. Routing and forecast are proved; search is not. Also seen: `Sir` capitalised again, the third time today. **The figure was then checked against an outside route, and it holds to 100 metres.** Both endpoints honest this time: the office end is the phone's own last fix read from `dumpsys location` (`22.576904, 88.434474`, ±20 m) and the home end is the plus code `R98C+6C7` decoded to `22.81556, 88.37106`. OSRM answers **40.3 km / 38 min**; the app said **40.2 km / 37 min**. Straight line is 27.3 km, so the road figure carries a 1.47× factor — a real route rather than a fudge. An earlier check that read 35.0 km was guessing town centres from area names, and the 5 km gap was the guess, not the app. **The routing half is now confirmed three ways**: against itself across two days and two phrasings, and against an outside route. What keeps this row `partial` is unchanged and is not distance — a TAVILY *search* answering has still never been verified, and a silent search failure still looks exactly like a confident answer. **Also read off the fix, and worth keeping:** `alt=-19.0` with `mslAlt=38.0` on the same reading — the ellipsoid figure and the mean-sea-level one, 57 m apart. The phone carries the sea-level altitude, and it is the honest one to show if the floor question comes back. **The search half is PROVED, 2026-09-01, 16:04 — the last gap on this row.** Asked for a figure that only a live lookup can hold: *\"what is the usd to inr rate right now\"*, answered *\"currently 95.44 Rupees, Sir… averaging around 95.46 over the last thirty days.\"* Checked against two outside sources within the minute: 95.20 from `open.er-api.com` and 94.91 from Google's own overview. **The test works because a weights-only answer would have been years stale** — the training-era rate is in the mid-eighties — so a confident invention and a real lookup are distinguishable here, which is precisely what this row said they were not in general. The thirty-day average is the second tell: that is read off a page, not recalled. Routing, forecast and search are all now proved from the phone.",
          "goal": "situated"
        },
        {
          "id": "situation-block",
          "name": "The situation sent to the persona",
          "status": "none",
          "blockedBy": "brain",
          "note": "Place, battery, link — one field. The highest character-per-line change available anywhere in the plan.",
          "goal": "situated"
        },
        {
          "id": "open-app",
          "name": "Opening an app on the phone by name",
          "status": "proved",
          "blockedBy": "none",
          "note": "Confirmed twice over: by `topResumedActivity` and by the target app’s own launch event. Needed a native build.",
          "goal": "acts"
        },
        {
          "id": "governance",
          "name": "Governance: parked actions, approve and deny",
          "status": "proved",
          "blockedBy": "none",
          "note": "Desk actions only.",
          "goal": "acts"
        },
        {
          "id": "deskwatch-clock",
          "name": "Desk-watch countdown, silence locks",
          "status": "proved",
          "blockedBy": "none",
          "note": "The desk owns the clock; the phone’s countdown is a readout, never a decision timer. Do not move it.",
          "goal": "acts"
        },
        {
          "id": "scripts-list",
          "name": "Scripts: list",
          "status": "proved",
          "blockedBy": "none",
          "note": "",
          "goal": "acts"
        },
        {
          "id": "scripts-crud",
          "name": "Scripts: create, update, delete, run by id",
          "status": "none",
          "blockedBy": "brain",
          "note": "Which is why editing is disabled.",
          "goal": "acts"
        },
        {
          "id": "run-history",
          "name": "Run history",
          "status": "none",
          "blockedBy": "brain",
          "note": "Reports currently invents “Last run: 2h ago” from a fixture.",
          "goal": "acts"
        },
        {
          "id": "presence",
          "name": "Presence — awake but idle",
          "status": "none",
          "blockedBy": "brain",
          "note": "Inferred from socket state today.",
          "goal": "reachable"
        },
        {
          "id": "declared-rules",
          "name": "Declared rules (“tell me if I haven’t…”)",
          "status": "none",
          "blockedBy": "brain",
          "note": "Spec written and awaiting review. Its presence half rests on a measurement that no longer holds.",
          "goal": "acts"
        },
        {
          "id": "anticipate-v1",
          "name": "Anticipation v1 — noticed when you open the app",
          "status": "untested",
          "blockedBy": "none",
          "note": "Decided in code from the journal against its own baseline and the next departure. At most one a day, never the same subject twice running, silent by default. **Device pass 2026-08-24:** the WATCHING panel read `2 OF 3 READY`, and its counters had advanced since 08-21 — `4 MORE DAYS` to `3 MORE DAYS`, and Today from `SPOKEN` to `LISTENING`. The day gate and the baseline counters are therefore working across days, not merely rendering. It was confirmed speaking on 08-21; whether a given remark is *worth* making is a separate question from whether the machinery runs. **Widened on 2026-08-28, and the budget was widened first.** Three triggers became seven — the app that moved past its own usual (`appDeltas`), being somewhere well before the hour you are usually there, being absent from somewhere you usually are by now, and a typed departure that no longer matches the hour you are measurably gone by. The enabler was `spokenStore` keeping **a day per subject** rather than one subject: with one slot and one remembered subject, every added trigger made the app *less* likely to say the useful thing, because a dull observation spent the day exactly as fast as a sharp one. Daily cap unchanged, quiet hours unchanged, every remark still quotes its own figure and refuses to speak below its day floor. **Absence is matched to the same weekday**, which is the gateway nudge bug written down as a rule. **Unproved on the device:** the new triggers have never fired on the phone, and the arrival one cannot fire before 08:00 at all, since the quiet hours start there. **The widened set was seen on the phone on 2026-09-01, 08:11**, the morning after it shipped: *\"At Home early, sir — usually you are there by 10:49 AM.\"* That is the new `arrival` trigger (`hereEarly`), quoting the median it measured, `sir` spent once and lowercase, inside the quiet hours. **And it was WRONG, reported the same morning, which is the finding rather than the sighting.** He had been at Home all night and was leaving for the Office; nothing arrived, and 10:49 AM is not when he reaches Home. **Two defects, and the second one poisons the figure.** `hereEarly` never asks whether an arrival happened — it compares the clock against a median and speaks. And that median is not an arrival time at all: `noteSeen` writes a sighting whenever the app resolves a named place, so the first sighting of a day is the first time the app was OPENED there, which for the place you wake up in is unrelated to arriving. **The fix is that an arrival requires having been somewhere else** — at fire time, and in the baseline, which must count only days that began with a real arrival. `absentFrom` shares the flaw and the fix. **Rebuilt around routine and deviation, 2026-09-01, and unproved on the device.** The question is no longer \"did you reach a place early\" but \"what is different about today\", which is what the sighting log can actually answer. `usualPlaceAt` learns where he is on this weekday at this hour, from days rather than sightings so a morning of refreshing the app cannot outvote four Tuesdays. `leftEarly` is the sharp one and the shape he described — *the app knows I am at home till about 8:10 Mon–Fri* — and it fires only once he is demonstrably somewhere else, so it reports an observed departure rather than guessing at a quiet morning. `elsewhereNow` names where he usually is instead of observing where he is not. **`hereEarly` survives as the weak signal it always was**: it now requires a stay that began today and began somewhere else, and it times the ARRIVAL rather than the moment somebody looked at the phone. `usuallyHereBy` counts arrivals only — the old median was of first-app-opens, which is what produced 10:49 AM as a claim about somebody's own home. **The reported failure is pinned by a test**: at Home all night, leaving for the office, must stay silent. 66 tests across `timeline` and `routine`. **Weekends learn now, 2026-09-01.** History runs twelve weeks rather than four — four weeks is at most four Saturdays and the rules wanted four, so a weekend routine needed a flawless month and never formed — and where the exact weekday is still thin the routine falls back to the kind of day, weekend or weekday, never across the two. Only the most recent matching days are read, so a job left in July cannot argue with September.",
          "goal": "situated"
        },
        {
          "id": "anticipate-pocket",
          "name": "Anticipation that finds you in your pocket",
          "status": "none",
          "blockedBy": "brain",
          "note": "Needs the gateway push or a foreground service — the phone measurably cannot do it alone.",
          "goal": "reachable"
        },
        {
          "id": "anticipate-habit",
          "name": "Anticipation from learned habit rather than a written rule",
          "status": "none",
          "blockedBy": "device",
          "note": "**Blocked by time, not by work, and the spec says so outright:** *\"Two to four weeks of baseline before 'unusual' means anything, and the senses in §3.2 to notice with. No amount of code shortens the first.\"* (`docs/superpowers/specs/2026-08-21-anticipation-design.md`.)\n\n**Two things worth separating, checked 2026-08-27.** First, *more triggers* is not this row. `anticipate-v1` has three — place, screen time, pickups — and a fourth is buildable today, since `rollup.ts` already computes `usual.top` per-app baselines beside `today.top`. That would be a fourth **hand-written rule over measured figures**, which the spec explicitly calls *not the same thing as a machine that learns your habits*. Building it strengthens `anticipate-v1` and leaves this row exactly where it is.\n\nSecond, and not previously recorded: **the baseline this row waits on is currently biased.** Sightings happen only when the app is opened (`timeline.ts` says so), so weeks of it would be weeks of a sample taken whenever he happened to look at his phone. So this is downstream of `ACCESS_BACKGROUND_LOCATION` in queue 23 as well as of the calendar — waiting three more weeks on the present sampling would not produce the baseline this row means. **2026-08-28:** the seven triggers now shipping are all rule-shaped and none of them shortens this. What did change is that four of them are measured against a baseline the phone builds itself, so the biased-baseline problem below is now load-bearing for more than one row: every sighting still needs the app to be open, which is queue 23 and background location.",
          "goal": "situated"
        },
        {
          "id": "place-map",
          "name": "The circles, drawn to scale, on Home",
          "status": "proved",
          "blockedBy": "none",
          "goal": "situated",
          "note": "**Built and seen on the phone, 2026-09-01.** Asked for as a way to see the match radius and the overlap rather than be told about it: the named places to scale, the 120 m circles, the reading as a dot with its accuracy as a dashed ring, a scale bar, and a caption that says outright when two circles touch. `react-native-svg`, which was already a dependency, so it ships over the air — every real map on Android is a native module. **Only what is within 800 m is drawn**, and the rest is counted in the caption: fitting all ten named places put 150 m in a pixel and the circles rendered at under one, which is how it first appeared on the phone. **Road tiles and a 3D tilt were built here and removed on request the same day** — both worked; the tiles cost a watermark once CARTO wanted a key, and the tilt turned the circles into ellipses, which ruins the comparison the panel exists for. Confirmed on the device at the Office: two circles 700 m apart, not touching, with 8 places counted out."
        }
      ]
    },
    {
      "name": "Platform",
      "rows": [
        {
          "id": "screens-tests",
          "name": "19 screens, full suite and clean typecheck",
          "status": "proved",
          "blockedBy": "none",
          "note": "The number written in a doc goes stale — trust the run, not the comment.",
          "goal": "product"
        },
        {
          "id": "release-apk",
          "name": "Standalone release APK, no bundler attached",
          "status": "proved",
          "blockedBy": "none",
          "note": "",
          "goal": "product"
        },
        {
          "id": "haptics-motion",
          "name": "Haptics, and an animation switch that works",
          "status": "proved",
          "blockedBy": "none",
          "note": "",
          "goal": "product"
        },
        {
          "id": "ota",
          "name": "OTA updates",
          "status": "proved",
          "blockedBy": "none",
          "note": "**Verified 2026-08-24.** Channel Active, branch linked, runtime matching the installed APK, latest group is HEAD. A **local** build still receives nothing unless the fingerprint is baked by hand — `prebuild` writes the literal placeholder and nothing logs a word about it.",
          "goal": "product"
        },
        {
          "id": "contrast",
          "name": "Contrast checked against WCAG AA",
          "status": "proved",
          "blockedBy": "none",
          "note": "4.78 over the floor and 4.68 over the navy crown, against a bar of 4.5.",
          "goal": "product"
        },
        {
          "id": "theme-removed",
          "name": "A theme choice that did nothing — removed",
          "status": "proved",
          "blockedBy": "none",
          "note": "Dark and System were identical. A setting that does nothing is worse than an absent one.",
          "goal": "product"
        },
        {
          "id": "arm64",
          "name": "arm64-only build",
          "status": "proved",
          "blockedBy": "none",
          "note": "A config plugin, registered — so `prebuild` cannot silently revert it, which is this project’s most expensive recurring bug class. Roughly 35 MB against 100.7 MB across four ABIs. **The old ledger listed this as owed; it is done.**",
          "goal": "product"
        },
        {
          "id": "reduce-motion",
          "name": "Motion defaulted from the OS",
          "status": "untested",
          "blockedBy": "device",
          "note": "Implemented at `src/theme/appearance.tsx:89` and spied on in a test; a deliberate toggle outranks it permanently. **The old ledger called this untested for want of code; it has code and a test, and owes only a device pass.**",
          "goal": "product"
        },
        {
          "id": "light-theme",
          "name": "Light theme",
          "status": "broken",
          "blockedBy": "app",
          "note": "Broken by design — “System” behaves identically to Dark. Decide or remove.",
          "goal": "product"
        },
        {
          "id": "appearance-persist",
          "name": "Appearance surviving a launch",
          "status": "untested",
          "blockedBy": "app",
          "note": "**Built 2026-08-31 and never launched on the phone.** `src/theme/appearanceStore.ts` keeps accent, glow and motion in AsyncStorage; the provider reads them once through `live()` and only writes after that read, so an empty disk cannot overwrite the real look during the first render. **Motion is tri-state on disk and that is the whole subtlety:** `null` means the switch was never touched and the OS keeps deciding, so a stored look cannot silently override reduced motion for somebody who asked their phone for less of it. 12 tests. The row claimed this for days while it was in-memory, which is why the claim now names the file. **The bundle carrying this landed on the phone at 18:42 on 2026-08-31** (update `01a057f3`, runtime `31c64113`), so this is now testable rather than theoretical — Settings → Updates reads the publish time, and Diagnostics is visible in the Settings list.",
          "goal": "product"
        },
        {
          "id": "crash-reporting",
          "name": "Crash and error reporting",
          "status": "untested",
          "blockedBy": "app",
          "note": "**The JavaScript half is built, 2026-08-28, and has never run on the phone.** `src/lib/crashLog.ts` keeps a derived record — name, message, redacted frames, and which build it happened on — five deep in AsyncStorage. `ErrorBoundary` writes the render crash it already displayed, and `installCrashHandler` wraps the global handler for everything outside render, calling the one it replaced so the app still dies exactly as it would have. Settings → Diagnostics reads them back, copies a report, and carries a count of what has not been looked at. **Redaction has a test per rule**, because an error message quotes its own input: a parse failure on a gateway frame would otherwise carry that frame — token and chat text included — into the one store whose purpose is to be pasted into a chat window. **Unproved on the device, and shippable over the air** — the runtime is still `31c64113`, checked after the change. **The native half is unchanged and still owed:** a native crash takes the process with no JS involved, so nothing here sees it; that is a service and a build, and belongs to queue 23.",
          "goal": "product"
        },
        {
          "id": "keystore",
          "name": "A real release keystore",
          "status": "none",
          "blockedBy": "app-build",
          "note": "Signed with Expo’s generated debug keystore, which is why a local APK and an EAS-signed one cannot replace each other.",
          "goal": "product"
        },
        {
          "id": "screen-reader",
          "name": "Screen-reader pass",
          "status": "none",
          "blockedBy": "device",
          "note": "Targets are floored at 44–64px and roles are set; nothing has been checked with TalkBack.",
          "goal": "product"
        },
        {
          "id": "tablet",
          "name": "Tablet and landscape",
          "status": "none",
          "blockedBy": "app",
          "note": "Locked to portrait. The reactor and the 2×2 grid need a breakpoint before a tablet is claimed.",
          "goal": "product"
        }
      ]
    }
  ],
  "timeline": [
    {
      "date": "2026-08-10",
      "title": "The HUD, and the shape of the thing",
      "kind": "built",
      "detail": "19 screens, five tabs, the reactor. One reducer for the link so every tab agrees about what is connected."
    },
    {
      "date": "2026-08-13",
      "title": "Named places and located answers",
      "kind": "built",
      "detail": "Coordinates resolved on the phone, because `KnownPlace` exists nowhere else. Unverified since the fix that touched it."
    },
    {
      "date": "2026-08-19",
      "title": "The journal, and derived facts only",
      "kind": "built",
      "detail": "Usage read on the device, rolled up, and shared as a rollup rather than rows. The pattern every later sense copies — and the reason a notification listener is designable at all."
    },
    {
      "date": "2026-08-19",
      "title": "Every state must name itself",
      "kind": "rule",
      "detail": "It came up five times in one day. The briefing that never ran and the briefing that ran and found nothing to say were indistinguishable from outside, which let a wrong hypothesis stand for a day."
    },
    {
      "date": "2026-08-20",
      "title": "Background work: measured, not assumed",
      "kind": "measured",
      "detail": "`expo-background-task` hardcodes `setRequiredNetworkType(CONNECTED)`, and the uid reads `blocked=REASON_APP_BACKGROUND|REASON_APP_STANDBY` with `#netAvail=0` in a RARE bucket. **Stopped, not deferred.** Every presence decision since is chosen around this."
    },
    {
      "date": "2026-08-21",
      "title": "He speaks first — and gets it wrong",
      "kind": "broke",
      "detail": "The first unprompted remark asserted a Saturday shift on a Friday. A bare substring test matched a Mon–Fri work pattern, and the prompt then asserted it as true today. Fixed in the brain the same day; still undeployed."
    },
    {
      "date": "2026-08-21",
      "title": "The briefing arrives twice",
      "kind": "broke",
      "detail": "Naming Home armed both senders at once — the gateway push and the phone's own task, byte-identical strings so the shade could not tell them apart. The old plan said *keep the local task as a fallback*; it was never gated, so it was a second sender."
    },
    {
      "date": "2026-08-21",
      "title": "A local build silently breaks OTA",
      "kind": "trap",
      "detail": "`prebuild` writes the literal `file:fingerprint` into `strings.xml`; EAS substitutes the real hash and a local Gradle build does not. The APK then asks for updates matching a placeholder, gets none, and runs its embedded bundle forever. Nothing logs a word. Cost an hour."
    },
    {
      "date": "2026-08-21",
      "title": "The status panel, and anticipation v1",
      "kind": "built",
      "detail": "Eight seams, four states, assembled on the device so it reads with nothing connected — which is exactly when it is looked at. Anticipation decides in code from the journal against its own baseline."
    },
    {
      "date": "2026-08-24",
      "title": "The OTA channel gate closed",
      "kind": "proved",
      "detail": "One command, read-only. Channel Active, branch linked, runtime byte-identical to the installed APK. The fear was an empty channel — the app asks and gets nothing, silently. It is not empty."
    },
    {
      "date": "2026-08-24",
      "title": "One source of truth for status",
      "kind": "built",
      "detail": "Three files were carrying status claims and drifting. Now `docs/status/ledger.json` holds them all and everything else is generated, so a percentage cannot disagree with the row it came from."
    },
    {
      "date": "2026-08-24",
      "title": "The briefing row stops asserting what it cannot know",
      "kind": "fixed",
      "detail": "A workspace-only week ages the upload stamp, and the panel read that as the gateway having lost the schedule. Three states now. The task's gate is untouched — a stale stamp still means the phone posts."
    },
    {
      "date": "2026-08-24",
      "title": "An npm script changes the runtime fingerprint",
      "kind": "trap",
      "detail": "`packageJson:scripts` is a fingerprint input. Adding two convenience scripts moved the runtime from `31c64113` to `3f9be979`, and the first OTA publish went to a runtime **no device has** — arriving nowhere, with a success message. Caught by reading the publish output against the installed fingerprint. The scripts were removed; the generator is invoked directly."
    }
  ],
  "queue": [
    {
      "n": 0,
      "title": "Confirm the OTA channel",
      "state": "proved",
      "blockedBy": "none",
      "detail": "Was one command and gated every JS-only fix behind it. Channel `production` Active, branch linked, runtime byte-identical to the installed APK. **Proved end to end:** the publish that followed landed on the matching runtime, which is the only thing this check ever claimed.",
      "goal": "product"
    },
    {
      "n": 1,
      "title": "The briefing-schedule row stops lying",
      "state": "shipped",
      "blockedBy": "device",
      "detail": "A workspace-only week ages the upload stamp, and the panel asserted the gateway lost the schedule — which the phone cannot know. Three states now: `AT THE GATEWAY`, `CANNOT TELL`, `ON THIS PHONE`. `cloudArmedState()` sits beside `cloudArmed()`, so the task gate is untouched and a stale stamp still means the phone posts. 10 tests; committed `d5eff11`; published to runtime `31c64113`. **On the phone 2026-08-24:** the new bundle is live (the update server serves it for this exact runtime and the app reports nothing newer), the panel renders, and the briefing row reads `AT THE GATEWAY` correctly while the link is cloud. **Still owed:** the new `CANNOT TELL` state itself is unreachable on a cloud-linked phone — the stamp is fresh — so it needs two days of workspace-only sessions or a debug build.",
      "goal": "reachable"
    },
    {
      "n": 2.5,
      "title": "The chat log in the right order, once each",
      "state": "proved",
      "blockedBy": "none",
      "detail": "**The ordering half is fixed; the duplicate half already was.** The note this item carried was stale — it asked for the real `at` values before more code, and `96de0e1` had already got them from a shipped audit and turned the exact-millisecond key into a 5-second window. What remained was that the chat rendered in ARRIVAL order: `ChatScreen` reversed the array without ever sorting by `at`, so a turn swept out of the tray carrying last night's stamp was appended last and read as the newest — yesterday's `15:xx` below today's `12:xx`, the second reported symptom. Fixed in the reducer rather than at the reader, because two readers assumed the array was ordered and `activity.ts` sorted the same data before showing it. 4 tests, 2 failed first; 999 tests green, `tsc` clean. **Closed 2026-08-27 on the phone**, on `01a0429e`, by scrolling the whole log rather than reading one screen — the previous fix here was called done from a screenshot and was not.",
      "goal": "conversation"
    },
    {
      "n": 2,
      "title": "Provider effects that outlive their tree",
      "state": "proved",
      "blockedBy": "none",
      "detail": "**Closed 2026-08-26.** An effect resolving after its test body corrupts the act environment, after which every later render returns an empty tree — no throw, no warning. It cost six tests. Fixed where `jarvisProvider.test.tsx` said it had to be: at the source, not inside each `.then`. `state/live.ts` is a per-run cancellation scope — a factory rather than a hook, because a provider-lifetime guard cannot cancel a dependency change and the alert registration at `[alert?.id, alert]` needs exactly that. All nine settle sites open one and end it from the effect cleanup; the one that hands a registration back on a dead run keeps its `l.alive` branch, named explicitly in a test so a second exception cannot appear quietly. The inventory is the argument for a primitive: eight sites carried a hand-written `let alive` and the ninth did not, and nothing could have told you which. 13 tests — 8 on the scope, 5 on the provider, including a source scan that fails if a bare settle reappears. Proved by mutation: reinstating the bare `.then` fails the scan, restoring it passes. No device needed, and none used.",
      "goal": "product"
    },
    {
      "n": 3,
      "title": "A crash says what it was",
      "state": "open",
      "blockedBy": "app",
      "detail": "**The JS half is written and green as of 2026-08-28. Nothing has been published, and nothing has been seen on the phone.** A crash used to restart in silence, with `adb logcat` on the one machine that built the APK as the only diagnosis. Now: a derived record — no chat text, no token, because a crash log is exactly what gets pasted into a chat window — written by the boundary for a render crash and by a wrapped global handler for everything else, read back at Settings → Diagnostics with a count on the row for what has not been looked at. **What is left is a publish and a look at the phone.** A native crash still needs a service and a build, and stays owed rather than quietly folded in.",
      "goal": "product"
    },
    {
      "n": 4,
      "title": "He answers out loud",
      "state": "open",
      "blockedBy": "app",
      "detail": "Voice out is the largest single gap in the chat, and app-only. The decision of whether and what to speak is pure and testable; the native call sits at the edge. Stop any utterance in flight before starting the next — two voices over each other is worse than none.",
      "goal": "voice"
    },
    {
      "n": 5,
      "title": "He comes back after a reboot",
      "state": "proved",
      "blockedBy": "none",
      "detail": "**Closed 2026-08-27 on the device, and it cost no code at all.** The phone was rebooted on the night of 26 Aug with the run count on Places at **22**, and the app was not opened afterwards. A briefing arrived at 07:00, and the count read **28** when the app was first opened that morning — six runs nobody started, from a process that had been killed by a reboot. WorkManager rescheduled its own queue at boot, which is exactly the bar `TESTING.md` 1.9 sets. By 10:27 it read 37. The premise this item was written on had already been corrected on 2026-08-26: `dumpsys package` on the installed APK says `RECEIVE_BOOT_COMPLETED: granted=true`, `expo-task-manager` declares a receiver on `BOOT_COMPLETED`, and there was never anything to build here — only a check nobody had run. **The 07:00 run cannot be read after the fact:** the heartbeat keeps one outcome, not a history, so nine later runs have overwritten whatever that one recorded.",
      "goal": "reachable"
    },
    {
      "n": 6,
      "title": "Speak into the microphone",
      "state": "blocked",
      "blockedBy": "brain",
      "detail": "**Run on 2026-08-27 and no longer the oldest unverified thing here.** Held the mic, said *\"hi jarvis\"*, and `brains.usage.audio` moved from all zeros to `gemini_ok: 1` with no fallback and no error — so the audio reaches the gateway and Gemini accepts the m4a. The mime worry was unfounded: `_AUDIO_MIME` already maps `m4a` to `audio/aac`. **What is left is accuracy, not plumbing:** the transcript read *\"ki service\"*, and the reply was correct Benglish for that question rather than for the one asked. `_GEMINI_TRANSCRIBE_PROMPT` primes for romanised Bengali by design and overcorrects on plain English — a gateway change, so it waits with the rest of the brain work.",
      "goal": "voice"
    },
    {
      "n": 7,
      "title": "The journal denial path, and the other never-run paths",
      "state": "open",
      "blockedBy": "device",
      "detail": "Settings → Journal → usage access off → return. It must say *I cannot see your usage*, never *Nothing recorded*. Built, tested, and never once run by a human. Same sitting: the location timeline and anticipation v1, both unexercised for the same reason.",
      "goal": "memory"
    },
    {
      "n": 8,
      "title": "Usable by someone who cannot see it well",
      "state": "open",
      "blockedBy": "device",
      "detail": "Better than the old ledger admitted: contrast is measured at 4.78 and 4.68 against a 4.5 bar, and reduced motion already follows the OS with a test on it. What is genuinely owed is the label audit and a TalkBack pass.",
      "goal": "product"
    },
    {
      "n": 9,
      "title": "The photo half of the chat",
      "state": "shipped",
      "blockedBy": "app",
      "detail": "**Two of the three parts were already built, which the queue did not know.** The thumbnail renders and a photo turn already marks itself `SENDING` / `NOT SENT`. What was genuinely missing, and is now built (2026-08-31), is recovery: a failed photo could not be re-sent, because the bytes went to the socket and only a local uri stayed in the log. It now re-encodes from that uri, keeps the caption, and says so plainly when the cached file is gone. Unpublished and unproved on the phone.",
      "goal": "conversation"
    },
    {
      "n": 10,
      "title": "Appearance surviving a launch",
      "state": "shipped",
      "blockedBy": "app",
      "detail": "**Built 2026-08-31.** Accent, glow and motion survive a launch, with motion stored tri-state so an untouched switch keeps following the OS. Cheap, ships over the air, and immediately visible — which is exactly why it mattered that the row claimed it for days while it was in-memory.",
      "goal": "product"
    },
    {
      "n": 11,
      "title": "The notification listener",
      "state": "blocked",
      "blockedBy": "brain",
      "detail": "The step change in presence — event-driven, nearly free, and more context than any polling can buy. **Gated on the token split, which is gateway work:** a listener sees OTPs, balances and private messages, and once that is collected badly no later fix un-collects it. What can be built now is the spec and the pure drop-rule reducer, with no service registered and no permission asked.",
      "goal": "reachable"
    },
    {
      "n": 12,
      "title": "Split APP_TOKEN by capability",
      "state": "blocked",
      "blockedBy": "brain",
      "detail": "One string gates the socket, push, `/app-commute` and `/app-state` alike, and nothing expires. The gateway validates both halves, so it cannot land here. The gate in front of every new sense, not a follow-up.",
      "goal": "security"
    },
    {
      "n": 13,
      "title": "Merge fix/durable-state, then deploy",
      "state": "proved",
      "blockedBy": "brain",
      "detail": "**Done, 2026-08-29.** `c86d176` is contained in `origin/feat/cloud-gateway`, so the briefing is no longer disarmed by a deploy and the false Saturday shift is live. Verified from this repo by `git branch -r --contains`, which is as much as this laptop can check.",
      "goal": "memory"
    },
    {
      "n": 14,
      "title": "The situation sent to the persona",
      "state": "blocked",
      "blockedBy": "brain",
      "detail": "Place, battery, link — one field on the envelope. The most character per line of code available anywhere in the plan, and it is a gateway change.",
      "goal": "situated"
    },
    {
      "n": 15,
      "title": "Delivered and read ticks",
      "state": "blocked",
      "blockedBy": "brain",
      "detail": "The phone knows *sent*. Only the gateway can say *delivered* and *read*, and it says neither. Needs a per-message id on both sides — and reply-to-a-message wants the same id, so it follows rather than inventing a second identity for a message.",
      "goal": "conversation"
    },
    {
      "n": 16,
      "title": "The desk-key handshake, and rotate BRIDGE_SECRET",
      "state": "blocked",
      "blockedBy": "desk",
      "detail": "18 sealed turns dropped and rising, so the encrypted path has never once worked end to end. The old secret went through a log and still opens `/desk-link`. **The only item whose cost grows while deferred** — do both in one sitting rather than waiting for the desk twice.",
      "goal": "security"
    },
    {
      "n": 17,
      "title": "Run run_harnesses.py",
      "state": "blocked",
      "blockedBy": "desk",
      "detail": "Never executed by a Python interpreter with the real imports. This laptop has no Python — `python` opens the Microsoft Store. A duplicated-call SyntaxError already slipped through once and was caught by reading.",
      "goal": "product"
    },
    {
      "n": 18,
      "title": "A real release keystore",
      "state": "blocked",
      "blockedBy": "app-build",
      "detail": "Release is signed with Expo's generated debug keystore, which is why a local APK and an EAS-signed one cannot replace each other. Fine for one phone, and the blocker on the second. **Folded into queue 23 on 2026-08-27** — it is free during a build that is happening anyway, and the spec sequences it with the other three.",
      "goal": "product"
    },
    {
      "n": 19,
      "title": "The Iron Man styling pass",
      "state": "open",
      "blockedBy": "app",
      "detail": "Scope is styling: the logo does not change and no copy changes. The pieces exist and are under-used. Land it on one screen and live with it for a day before propagating — the item most likely to sprawl. The Android glass blur stays last of all: it adds no capability and its failure mode is a segfault that takes the process.",
      "goal": "product"
    },
    {
      "n": 20,
      "title": "Promote what he is told into what he knows",
      "state": "open",
      "blockedBy": "app",
      "detail": "Today he learns four things, all about the phone: screen time, pickups, top apps, plus named places. He learns **nothing** from what you say — the only route from a sentence to a durable fact is typing it into the Memory screen yourself. Meanwhile the chat window holds about a day and rolls silently. The pattern to copy already exists and is proved: `shareFacts` derives, keeps a ledger of what it already sent, supersedes what changed, and — critically — sends **derived facts, never raw rows**. A turn-to-fact pass wants the same shape and the same restraint, plus a rule about consent, because quietly distilling everything a person says is a different product from one that remembers what it was told.",
      "goal": "memory"
    },
    {
      "n": 21,
      "title": "A day boundary in the chat",
      "state": "shipped",
      "blockedBy": "app",
      "detail": "**Already built, found 2026-08-31 by reading the screen rather than the row.** `DayRule` renders a rule with the day's name between one day's turns and the next (`ChatScreen.tsx`), placed on the oldest turn of each day, with `dayHeading` pinned by its own tests. Nothing was written for this today except this correction.",
      "goal": "conversation"
    },
    {
      "n": 22,
      "title": "The gateway briefing repeats itself too",
      "state": "blocked",
      "blockedBy": "brain",
      "detail": "**WRITTEN on the home machine 2026-08-29 (`02604c6`) and NOT LIVE.** Ported from this repo's `src/lib/briefingVoice.ts` line for line rather than reinvented, because two senders with two voices is worse than the repetition: a pool per slot, one cursor object shared across departures, the figures never varied, and the actionable word (`umbrella`, `jacket`, `Water`, `hair`, `wait it out`) kept in every variant, since Android truncates a body in the shade. The cursor is spent only on a briefing that actually went out — `_push_all` returns whether anything left, and a push that reached nobody now leaves the day OPEN rather than marking it done, which is the rule the failed-forecast path already had. It persists through `_persist(\"briefing_voice\")` and is restored on boot, the half that waited on durable state: a cursor a deploy resets restarts the rotation at the same line every time. `_briefing_text` without a cursor still draws the first line of every pool, so the prose the harness asserts is unchanged; `test_commute_briefing.py` 15 -> 26, negative-tested by committing the cursor before the push instead of after. **Owed: a deploy, then two mornings of looking at the notification.**",
      "goal": "reachable"
    },
    {
      "n": 23,
      "title": "The next native build — alarms, battery, background sightings, keystore",
      "state": "partial",
      "blockedBy": "none",
      "goal": "acts",
      "detail": "**Specced 2026-08-27, awaiting approval: `docs/superpowers/specs/2026-08-27-next-native-build-design.md`.** Prompted by *\"can the app set alarms?\"* — it cannot, and it says so honestly in the log. `AlarmClock.ACTION_SET_ALARM` would be perhaps forty lines, **and it cannot be published**: `android.permissions` is a fingerprint input, so one permission moves the runtime and a publish then reaches no device at all.\n\nSo the unit of work is the APK, not the feature, and **three other things have been waiting for one**: `expo-battery` for the missing half of §3.2.1, `ACCESS_BACKGROUND_LOCATION` for the `timeline` row's structural weakness (sightings only happen when the app is opened), and the real release keystore from queue 18, which is free during a build that is happening anyway. Spending a build on alarms alone and finding the next item a week later is the failure the spec exists to prevent.\n\n**Background location is the one to argue separately** — it is the only item that changes what the app knows while closed. The alarm design is deliberately narrow: recognised on the phone rather than by the model (`asOpenAppCommand`'s rule, since a model asked to set an alarm can only say it did), ambiguity declines — *\"set an alarm for 6:30\"* is twice a day and asks — and `EXTRA_SKIP_UI` stays false so a human confirms what a regex parsed.\n\n**2026-09-01: the background-sightings half shipped, and the build was local.** `expo-location` with `isAndroidBackgroundLocationEnabled`, `ACCESS_BACKGROUND_LOCATION`, and a WATCH control on Places. The OTA runtime moved `31c64113` to `1818e1b7`, which is why the code and the manifest were deliberately split across two sittings: the geofence library landed first and shipped over the air, and only the manifest change waited for a build.\n\n**Queue 18 was NOT taken in the same build, and the reason is data.** The installed app is signed with `android/app/debug.keystore` (cert `fac61745dc09`), so a build signed with any other key — EAS included — cannot update it. The only way to install one is to uninstall first, and that costs twelve weeks of sightings, the chat log and the journal. A real keystore now means either accepting that loss or doing signing-scheme v3 key rotation, which is a separate job. `adb install -r` with the same key keeps everything, and that is what was done.\n\nStill owed by this queue item: the alarm intent, and `expo-battery` for §3.2.1."
    },
    {
      "n": 24,
      "title": "The distance question answers with invented numbers",
      "state": "proved",
      "blockedBy": "none",
      "goal": "situated",
      "detail": "**FIXED on the home machine 2026-08-29, `efd1af1`, and unproved from the phone.** Both halves landed together as the handoff asked: the destination is preferred from a `to` phrasing and `here` / `my location` / `current location` are rejected, and the stale `currently` fact is no longer stored as permanent. **What is owed now is one question asked out loud from somewhere that is not home** — *\"how far is home from here\"* — which is the phrasing that was never tried and the whole reason this survived so long. **Closed 2026-09-01**: asked from the Office in the exact phrasing, answered with measured road distance and time."
    },
    {
      "n": 25,
      "title": "A spoken turn that outlives its socket arrives with no question above it",
      "state": "shipped",
      "blockedBy": "app-build",
      "goal": "conversation",
      "detail": "**BOTH HALVES ARE WRITTEN, on the home machine, 2026-08-29 — and neither is live.** Gateway `2301ad7` on `feat/cloud-gateway`: the transcript `emit()` result is read rather than discarded, and when the socket died before it left, the text rides on the reply's push as `data.transcript`. App half in this repo: `replyFromData` reads that field for `kind: reply` only, `take()` dispatches a `transcript` frame above the answer with the same `at`, and the reducer deduplicates a user turn the way it already did a pushed reply — the tray sweep re-enters the same notification on every return to the foreground, which would otherwise have appended his question again under its own answer. **What is owed is a deploy and a publish, not code:** Render has to take the gateway commit and the app half has to go out over the air before anyone can see it. Then the device proof, which is the original repro — ask by voice, pocket the phone, and read what the chat shows when it comes back. Tests: `test_app_link.py` 42 -> 46 (three drive the real handler with `send_json` failing on exactly the frame a pocketed phone loses; one pins the discarded-`emit` statement form a refactor would restore), and 1119 jest across 80 suites here, typecheck clean. **2026-08-31: both halves are committed and neither is on the phone.** The gateway half is live; the app half is `6d8be2d` here, waiting with everything else on an OTA publish. Until that publish, a spoken turn that outlives its socket still arrives as an answer with no question above it — which looks exactly like the original defect and is not it."
    }
  ],
  "anticipations": {
    "note": "What the phone can notice, and what each one costs. The rule every entry obeys: a remark names the figure it is arguing from, so a wrong estimate is arguable rather than authoritative — and silence is the default, since one remark a day is the whole budget.",
    "items": [
      {
        "id": "still-here-late",
        "name": "Still somewhere you are usually gone from",
        "says": "Still at Office, sir. You are usually gone by 6:40 PM.",
        "from": "Sightings, median of the last sighting each day",
        "needs": "Four days at that place. Nothing else",
        "state": "live"
      },
      {
        "id": "left-early",
        "name": "Gone earlier than this weekday goes",
        "says": "Last seen at Home at 7:05 AM, sir. On 4 days like this one you are there until 8:10 AM.",
        "from": "Sightings, weekday-matched, and where you are now",
        "needs": "Three matching days, and being demonstrably somewhere else",
        "state": "live"
      },
      {
        "id": "elsewhere-now",
        "name": "Somewhere your own weekdays say you are not",
        "says": "Around now, sir, 4 days like this one have you at Home.",
        "from": "The routine: where you are at this hour on this kind of day",
        "needs": "Three matching days, and a named place to be at",
        "state": "live"
      },
      {
        "id": "arrival-early",
        "name": "Arrived somewhere well before you usually do",
        "says": "At Office early, sir — usually you are there by 9:00 AM.",
        "from": "Sightings, arrivals only — a stay that began somewhere else",
        "needs": "Four arrivals. The weak one: arrival is barely observable without background location",
        "state": "live"
      },
      {
        "id": "absent-from",
        "name": "Missing from somewhere you usually are by now",
        "says": "Not at Office, sir. You are usually there by 9:00 AM.",
        "from": "Sightings, matched to the same weekday",
        "needs": "Four of that weekday, and an hour past the usual",
        "state": "live"
      },
      {
        "id": "app-moved",
        "name": "The app that moved, rather than the day that did",
        "says": "2h 40m in Instagram today against a usual 50m, sir.",
        "from": "The journal, per-app days",
        "needs": "Usage access, three days of history, and 45 minutes in the app",
        "state": "live"
      },
      {
        "id": "heavy-day",
        "name": "A day well past your own usual",
        "says": "4h on the phone today against a usual 1h 40m, sir.",
        "from": "The journal",
        "needs": "Usage access and three days",
        "state": "live"
      },
      {
        "id": "fidgety-day",
        "name": "Many more pickups than usual",
        "says": "120 pickups today against a usual 45, sir.",
        "from": "The journal, unlock events",
        "needs": "Usage access and three days",
        "state": "live"
      },
      {
        "id": "schedule-drift",
        "name": "A departure time that no longer matches you",
        "says": "Your Office departure is set for 9:00 AM, sir, and you were last seen there by 8:30 AM on 4 days.",
        "from": "The Places schedule against the sighting median",
        "needs": "Four measured days and a 30-minute gap",
        "state": "live"
      },
      {
        "id": "late-night",
        "name": "A night that ran much later than yours do",
        "says": "On the phone until 1:12 AM, sir — three hours past your usual last pickup.",
        "from": "The journal: the last unlock of each day",
        "needs": "Nothing new. Said the next morning, since the quiet hours forbid saying it live",
        "state": "possible"
      },
      {
        "id": "up-early",
        "name": "Awake earlier than you usually are",
        "says": "First pickup at 5:40 AM, sir — two hours before your usual.",
        "from": "The journal: the first unlock of each day, weekday-matched",
        "needs": "Nothing new",
        "state": "possible"
      },
      {
        "id": "commute-slow",
        "name": "A journey that took longer than it usually does",
        "says": "An hour and ten from Home to Office, sir. Usually forty minutes.",
        "from": "Sightings: the gap between last-seen at one place and first-seen at the next",
        "needs": "Nothing new, and it is the best use of the sighting log that is not yet built",
        "state": "possible"
      },
      {
        "id": "app-untouched",
        "name": "Something you use daily that you have not opened",
        "says": "Nothing in Gmail today, sir, against every other weekday this month.",
        "from": "The journal, per-app days",
        "needs": "Usage access. Risks nagging, so it wants a high bar before it speaks",
        "state": "possible"
      },
      {
        "id": "turn-never-sent",
        "name": "Something you asked that never left the phone",
        "says": "One thing you asked yesterday never went, sir.",
        "from": "The chat log: a turn still marked failed or interrupted",
        "needs": "Nothing new",
        "state": "possible"
      },
      {
        "id": "briefing-missed",
        "name": "A briefing that was due and did not run",
        "says": "No briefing ran this morning, sir. The schedule is armed and the task has not fired since Tuesday.",
        "from": "The task heartbeat, which already records every run",
        "needs": "Nothing new",
        "state": "possible"
      },
      {
        "id": "battery-before-leaving",
        "name": "Leaving with a battery that will not last the journey",
        "says": "12 per cent, sir, and you usually leave in twenty minutes.",
        "from": "Battery level against the departure",
        "needs": "expo-battery — a native module, so an APK",
        "state": "needs-build"
      },
      {
        "id": "floor-changed",
        "name": "Which floor you are on",
        "says": "Back at your desk, sir — six floors up.",
        "from": "A barometer, which reads relative height to about a metre",
        "needs": "expo-sensors and a calibration point. GPS height cannot do it: the error is tens of metres against a floor of three",
        "state": "needs-build"
      },
      {
        "id": "left-without-opening",
        "name": "Anything at all noticed while the phone is in your pocket",
        "says": "(any of the above, without the app being opened first)",
        "from": "Geofence enter and exit at named places",
        "needs": "ACCESS_BACKGROUND_LOCATION, so an APK. **This is the one that unbiases every figure above** — today a sighting only exists when the app is opened, which is why \"usually at Home by 10:49 AM\" was ever said",
        "state": "needs-build"
      },
      {
        "id": "reaches-you-in-pocket",
        "name": "A remark that finds you rather than waiting to be met",
        "says": "(delivery, not a new observation)",
        "from": "The phone decides, the gateway pushes",
        "needs": "A gateway route. The phone measurably cannot run unattended on this device",
        "state": "needs-gateway"
      }
    ]
  }
}
